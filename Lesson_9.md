Отлично! После интерактивных устройств переходим к автоматике. Датчик движения — простой, но очень эффектный компонент. Вот подробное руководство для **занятия 9**.

---

### **Занятие 9: Датчик движения PIR — электронный сторож**

🎯 **Цель занятия:**
Научить Arduino "видеть" движение! Узнаем, как работает пассивный инфракрасный датчик (PIR), научимся настраивать его чувствительность и время задержки, создадим несколько полезных автоматических систем от простой сигнализации до "умного" освещения.

---

### 📖 **Теоретическая часть: Как датчик видит невидимое?**

**1. Что такое PIR?**
PIR (Passive InfraRed) — **пассивный инфракрасный** датчик.
*   **Пассивный** — значит он сам **не излучает** ничего, а только улавливает (в отличие от ультразвукового дальномера, который "кричит").
*   **Инфракрасный** — значит он "видит" **тепловое излучение**. Все теплые объекты (человек, животное, батарея) излучают инфракрасные волны.

**2. Как он обнаруживает движение?**
Внутри датчика есть специальная **линза Френеля** (такая ребристая пластмассовая штука). Она разбивает пространство перед датчиком на **сектора** (как пчелиные соты).
*   Когда в зоне видимости нет движения, инфракрасный фон **стабилен**.
*   Когда теплый объект (например, человек) перемещается из одного сектора в другой, датчик фиксирует **изменение** ИК-излучения и срабатывает.

**3. Основные настройки датчика (два потенциометра)**
На плате датчика HC-SR501 обычно есть два "крутильных" регулятора:
*   **SENSITIVITY (чувствительность):** Регулирует **дальность** (обычно 3-7 метров) и **размер объекта**, на который будет реакция. По часовой стрелке — увеличение.
*   **TIME (время задержки):** Определяет, **как долго** будет держаться высокий сигнал на выходе после обнаружения движения. От **0.3 секунды до 5 минут**. По часовой стрелке — увеличение.

**4. Режимы работы (перемычка)**
*   **H (Repeatable Trigger):** При обнаружении движения выход включается. Если движение продолжается, выход **будет оставаться включенным всё время**. После прекращения движения выход выключится через установленное время.
*   **L (Single Trigger):** При обнаружении движения выход включается **строго на установленное время**, даже если движение продолжается. После этого датчик "засыпает" на пару секунд.
*   *Чаще всего используется режим **H**.*

**5. Подключение (проще некуда)**
У датчика всего 3 вывода:
*   **VCC** — Питание (**5V**). *Важно: многие датчики НЕ РАБОТАЮТ от 3.3V!*
*   **GND** — Земля.
*   **OUT** — Цифровой выход. В состоянии покоя — **LOW (0V)**, при обнаружении движения — **HIGH (5V)**.

![Схема подключения PIR-датчика](https://i.imgur.com/JGmC0g3.png)

🔗 **Полезные ресурсы:**
*   [Подробный гайд по PIR от Алекса Гайвера](https://alexgyver.ru/lessons/pir/)
*   [Как работает линза Френеля (видео)](https://youtu.be/YKzA5rJGojo)
*   [Официальная документация на датчик HC-SR501](https://www.alldatasheet.com/datasheet-pdf/pdf/1132108/ETC2/HC-SR501.html)

---

### 🛠 **Практическая часть: "Электронный сторож" со световой и звуковой индикацией**

**📦 Что нужно:**
*   Arduino Uno / Nano
*   Макетная плата
*   Датчик движения PIR (HC-SR501)
*   Светодиод
*   Резистор 220 Ом
*   Пьезо-зуммер (пассивный)
*   Провода «папа-папа»

**🔌 Собираем схему**
Подключи компоненты по следующей схеме:

```
        PIR Датчик (HC-SR501)             ARDUINO UNO             Доп. компоненты
        ----------------------            -----------             ---------------
        | VCC | OUT | GND |               |         |             |              |
        ----------------------            -----------             ---------------
           |      |     |                  |     |                 |         |
           |      |     |                  |     |                 |         |
           |      |     |                  |     |                 |         |
           |      |     ------------------>| GND |                 |         |
           |      |                        |     |                 |         |
           |      ------------------------>| D2  | (цифр. вход)    |         |
           |                               |     |                 |         |
           ------------------------------->| 5V  | (питание)       |         |
                                                                   |         |
        СВЕТОДИОД (с резистором!)                                  |         |
        Длинная нога (Анод) ---------------------------------------|> D13    |
        Короткая нога (Катод) ------------------------------------>| GND     |
                                                                   |         |
        ЗУММЕР (пассивный)                                         |         |
        Красный провод (+) ----------------------------------------|> D8     |
        Черный провод (-) -----------------------------------------|> GND    |
```

**💻 Код: "Датчик движения с индикацией"**
Этот код будет считывать состояние датчика, зажигать светодиод и издавать звук при обнаружении движения. Также мы добавим счетчик срабатываний и выведем всё в Монитор порта.

```cpp
// Пины для подключения компонентов
const int pirPin = 2;     // Пин датчика PIR (OUT)
const int ledPin = 13;    // Встроенный светодиод + наш внешний
const int buzzerPin = 8;  // Пин зуммера

// Переменные для логики
bool motionDetected = false; // Флаг: обнаружено ли движение сейчас
bool lastMotionState = false; // Предыдущее состояние датчика
unsigned long motionStartTime = 0; // Время начала последнего движения
int motionCount = 0;        // Счетчик всех срабатываний

// Настройка временных параметров
const unsigned long BUZZER_DURATION = 1000; // Звуковой сигнал - 1 секунда
const unsigned long ALERT_DELAY = 5000;     // Пауза между оповещениями - 5 сек

void setup() {
  // Инициализация последовательного порта для отладки
  Serial.begin(9600);
  Serial.println("=== СИСТЕМА ОХРАНЫ НА PIR ===");
  Serial.println("Ожидание калибровки датчика...");

  // Настройка пинов
  pinMode(pirPin, INPUT);
  pinMode(ledPin, OUTPUT);
  pinMode(buzzerPin, OUTPUT);

  // Изначально всё выключено
  digitalWrite(ledPin, LOW);
  noTone(buzzerPin); // Гасим звук, если был

  // Датчику PIR нужно 30-60 секунд на калибровку при первом включении!
  // Он запоминает фоновую ИК-картину помещения.
  delay(30000); // Ждем 30 секунд
  Serial.println("Калибровка завершена. Система активна.");
  Serial.println("-----------------------------");
}

void loop() {
  // 1. Считываем текущее состояние датчика (HIGH = движение)
  bool currentMotionState = digitalRead(pirPin);

  // 2. Обнаружили изменение состояния (с LOW на HIGH)
  if (currentMotionState && !lastMotionState) {
    motionDetected = true;
    motionStartTime = millis(); // Засекаем время начала движения
    motionCount++; // Увеличиваем счетчик

    // Визуальная индикация (светодиод)
    digitalWrite(ledPin, HIGH);

    // Звуковая индикация (короткий сигнал)
    tone(buzzerPin, 1000, BUZZER_DURATION); // Частота 1000 Гц, длительность 1 сек

    // Вывод информации в монитор порта
    Serial.print("ДВИЖЕНИЕ! #");
    Serial.print(motionCount);
    Serial.print(" | Время: ");
    printTime(millis());
  }

  // 3. Если движение было, но сейчас его нет (с HIGH на LOW)
  if (!currentMotionState && lastMotionState) {
    motionDetected = false;
    digitalWrite(ledPin, LOW); // Гасим светодиод
    Serial.println("Движение прекратилось.");
  }

  // 4. Если движение продолжается дольше ALERT_DELAY - повторное оповещение
  if (motionDetected && (millis() - motionStartTime > ALERT_DELAY)) {
    Serial.println("ВНИМАНИЕ: Движение продолжается слишком долго!");
    motionStartTime = millis(); // Сбрасываем таймер для следующего оповещения
    
    // Можно добавить другой звук или мигание
    // tone(buzzerPin, 1500, 500); // Более высокий тон
  }

  // 5. Сохраняем состояние для следующего цикла
  lastMotionState = currentMotionState;

  // Небольшая задержка для стабильности
  delay(50);
}

// Вспомогательная функция для красивого вывода времени
void printTime(unsigned long milliseconds) {
  unsigned long seconds = milliseconds / 1000;
  unsigned long minutes = seconds / 60;
  unsigned long hours = minutes / 60;
  
  Serial.print("[");
  if (hours < 10) Serial.print("0");
  Serial.print(hours);
  Serial.print(":");
  if (minutes % 60 < 10) Serial.print("0");
  Serial.print(minutes % 60);
  Serial.print(":");
  if (seconds % 60 < 10) Serial.print("0");
  Serial.print(seconds % 60);
  Serial.println("]");
}
```

**🔍 Что делать после загрузки кода:**
1.  Открой **Инструменты -> Монитор порта** (9600).
2.  **Отойди от датчика на 1.5-2 метра** и не двигайся 30 секунд. Датчик калибруется!
3.  После сообщения "Система активна" пройди перед датчиком.
4.  Наблюдай: светодиод загорится, зуммер запищит, в мониторе порта появится сообщение.
5.  Поэкспериментируй с **потенциометрами** на датчике:
    *   **Sensitivity:** Покрути, попробуй сработать с большего расстояния.
    *   **Time:** Установи минимум, потом максимум — движение перед датчиком будет показывать разную длительность свечения светодиода.

---

### 🧠 **Разбор кода: Тайминги и логика автоматов**

**1. Калибровка датчика — это важно!**
`delay(30000);` в `setup()` — не просто задержка. Датчику PIR нужно **запомнить фоновую тепловую картину** комнаты (стены, мебель). Без этого он будет срабатывать ложно. В реальных проектах часто пишут на экране "Калибровка..." и отсчитывают 30 секунд.

**2. Функция `millis()` вместо `delay()` для параллельных задач**
Мы используем `millis()` для отсчета времени. Почему не `delay()`?
*   `delay(5000)` **остановил бы всю программу** на 5 секунд. В это время мы не смогли бы обнаружить новое движение!
*   `millis()` позволяет делать несколько вещей "параллельно": следить за датчиком И отсчитывать время для повторного оповещения.

**3. Обнаружение фронта сигнала**
Как и с кнопкой джойстика, мы сравниваем текущее и предыдущее состояние:
```cpp
if (currentMotionState && !lastMotionState) {
  // Это МОМЕНТ, когда движение только что обнаружено
}
```
Это позволяет выполнить код **ровно один раз** в момент начала движения.

**4. Функция `tone()` для звука**
`tone(pin, frequency, duration)` — генерирует меандр (квадратный сигнал) заданной частоты на указанном пине.
*   Частота 1000 Гц = высокий писк, 200 Гц = низкий гудок.
*   `noTone(pin)` — выключает звук.

**5. Собственная функция `printTime()`**
Мы создали свою функцию для красивого форматирования времени. Это хорошая практика — выносить повторяющийся или сложный код в отдельные функции.

---

### 🚀 **Твои проекты на основе датчика движения**

**🔹 Проект 1: "Умный ночник с плавным включением"**
*   **Цель:** Создать комфортное автоматическое освещение.
*   **Что нужно:** PIR-датчик + **несколько светодиодов на ШИМ-пинах** (например, D3, D5, D6) или **светодиодная лента через MOSFET**.
*   **Задача:**
    1.  При обнаружении движения свет **плавно** разгорается до максимальной яркости за 1-2 секунды (используй `for`-цикл или `millis()` с `analogWrite()`).
    2.  Если движение продолжается, свет горит на максимуме.
    3.  После прекращения движения таймер (3-10 секунд, настраивается потенциометром на PIR) начинает отсчет, и свет **плавно гаснет**.
    4.  **Усложнение:** Добавь **фоторезистор** (будем изучать позже) или **датчик освещенности**, чтобы ночник включался только когда в комнате темно.
*   **Совет:** Используй переменную для хранения целевой яркости и плавно изменяй текущую яркость к целевой в каждом цикле `loop()`.

**🔹 Проект 2: "Сигнализация с дистанционным управлением и дисплеем"**
*   **Цель:** Создать полноценную охранную систему с интерфейсом.
*   **Что нужно:** PIR + **OLED-дисплей** + **3 кнопки** (или джойстик с кнопкой для меню).
*   **Задача:**
    1.  Реализуй **меню на OLED**: "Вкл/Выкл", "Чувствительность", "Время задержки".
    2.  **Кнопка 1** — Постановка/снятие с охраны. При постановке на экране обратный отсчет 30 сек на калибровку.
    3.  **Кнопки 2 и 3** — Настройка параметров прямо через Arduino (вместо кручения потенциометров на датчике!).
    4.  При срабатывании сигнализации:
        *   На экране большими буквами "ALARM!" и счетчик срабатываний.
        *   **Мощный зуммер** включается прерывисто (би-би-бип).
        *   Загорается **красный светодиод**.
    5.  **Бонус:** Добавь запись времени срабатываний в массив с последующим выводом на экран.
*   **Совет:** Для меню создай глобальную переменную `int menuState = 0;` и переключай её состояния по нажатию кнопок.

**🔹 Проект 3: "Система подсчета посетителей"**
*   **Цель:** Создать устройство для подсчета людей, входящих в помещение.
*   **Что нужно:** PIR + **два светодиода (зеленый, красный)** + **OLED-дисплей** или **7-сегментный индикатор через сдвиговый регистр 74HC595**.
*   **Логика работы:**
    1.  Датчик направлен на дверной проем.
    2.  Человек входит → датчик срабатывает → **зеленый светодиод** мигает, счетчик **увеличивается** на 1.
    3.  Человек выходит → датчик срабатывает → **красный светодиод** мигает, счетчик **уменьшается** на 1.
    4.  Текущее количество людей отображается на дисплее.
*   **Проблема и решение:** Один датчик не различает "вход" и "выход". Нужно **два PIR-датчика**, расположенных на расстоянии 30-50 см друг от друга.
    *   **Последовательность срабатывания:**
        *   Сначала датчик 1, потом датчик 2 → **ВХОД** (+1)
        *   Сначала датчик 2, потом датчик 1 → **ВЫХОД** (-1)
    *   Используй логику с временными метками (`millis()`) и флагами для определения направления.
*   **Совет:** Это самый сложный проект. Начни с одного датчика и простого счетчика, затем добавь второй и реализуй логику определения направления.

---

### ❓ **Вопросы на засыпку**

1.  Почему датчик PIR называется **пассивным**? Чем он принципиально отличается от ультразвукового датчика HC-SR04?
2.  Что будет, если **не дать датчику 30 секунд на калибровку** при включении? Почему?
3.  В каком режиме (H или L) должен работать датчик для проекта "Умный ночник"? А для "Сигнализации"? Объясни свой выбор.
4.  Почему датчик может срабатывать **ложно** на: а) сквозняк из окна, б) включенный обогреватель, в) мигающую лампочку? (Подсказка: подумай, что излучает ИК).
5.  Придумай, как можно использовать **один PIR-датчик** для грубого определения **направления** движения (слева направо или справа налево). Подсказка: подумай о времени нахождения объекта в зоне видимости.

---

### 📌 **Критерии успеха: как понять, что ты молодец?**

*   ✅ Схема собрана, базовый код работает: светодиод и зуммер реагируют на движение.
*   ✅ Ты набрался терпения и дал датчику **полные 30 секунд на калибровку**.
*   ✅ Поэкспериментировал с потенциометрами SENSITIVITY и TIME, понял их влияние.
*   ✅ В Мониторе порта появляются четкие сообщения о начале и конце движения.
*   ✅ Ты выбрал один из проектов (или свой) и реализовал **хотя бы одну дополнительную функцию** (плавность, меню, счетчик).

---

### 🆘 **Скорая помощь: если что-то пошло не так...**

*   **Датчик не реагирует на движение вообще:**
    *   **Калибровка!** Ты точно ждал 30 секунд, отошел и не двигался?
    *   Проверь **режим перемычки** (джампера). Поставь в положение **H**.
    *   Покрути потенциометр **SENSITIVITY** по часовой стрелке до упора (макс. чувствительность).
    *   Проверь подключение **OUT** к цифровому пину Arduino.
*   **Датчик срабатывает сам по себе (ложные срабатывания):**
    *   Убеди от датчика **батареи, обогреватели, кондиционеры, вентиляторы** — всё, что создает поток теплого/холодного воздуха.
    *   Уменьши чувствительность потенциометром **SENSITIVITY**.
    *   Направь датчик так, чтобы в его зоне не было окон.
*   **Светодиод горит постоянно, даже без движения:**
    *   Проверь, не перепутал ли ты **OUT** и **VCC** на датчике?
    *   Возможно, потенциометр **TIME** выкручен на максимум. Поверни его против часовой стрелки.
    *   Перезагрузи Arduino и снова пройди процедуру калибровки.
*   **Зуммер не пищит:**
    *   Убедись, что это **пассивный** зуммер (умеет играть разные ноты с `tone()`), а не активный (пищит только при подаче питания).
    *   Проверь полярность: "+" зуммера — в цифровой пин Arduino, "-" — в GND.
*   **Проект с двумя датчиками работает некорректно:**
    *   Расположи датчики **вплотную друг к другу**, чтобы одна нога человека пересекала оба последовательно.
    *   Добавь в код задержку (`delay(10)`) между чтением первого и второго датчика, чтобы избежать электрических помех.
    *   Увеличь временное окно для определения последовательности (например, если второй датчик сработал в течение 500 мс после первого — это одно движение).

---

**🎉 Потрясающе!** Теперь твой Arduino может реагировать на присутствие человека. Это основа для "умного дома", систем безопасности и автоматизации. Ты освоил важный тип датчиков — пассивные сенсоры.
