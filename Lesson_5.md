# **Занятие 5: Механическое движение — знакомство с сервоприводом**

---

## **🎯 Цель занятия:**
Научить Arduino **управлять физическим движением**! Сегодня ты превратишь свою плату в дирижёра механических систем. Ты освоишь работу с сервоприводом — ключевым компонентом для создания роботов, манипуляторов и систем автоматизации. Мы научимся управлять углом поворота, создавать плавные анимации и комбинировать сервопривод с другими компонентами.

---

## **📖 Теоретическая часть: От свечения к движению**

### **1. Что такое сервопривод и зачем он нужен?**
Сервопривод (сервомотор) — это **"умный" мотор**, который может поворачиваться на **заданный угол** и **удерживать это положение**. В отличие от обычного двигателя, который просто крутится, сервопривод имеет встроенную систему обратной связи, позволяющую точно контролировать положение вала.

**Где применяется:**
- Робототехника (суставы роботов, захваты)
- Авиамоделирование (рули самолётов)
- Умный дом (жалюзи, замки)
- Промышленность (клапаны, заслонки)

### **2. Устройство сервопривода SG90 (самого популярного)**
![Сервопривод SG90](https://alexgyver.ru/wp-content/uploads/2020/05/servo_pins.jpg)

*   **Состоит из:**
    1.  **Двигатель постоянного тока** — создаёт вращение
    2.  **Редуктор** — уменьшает скорость, увеличивает силу
    3.  **Потенциометр** — измеряет текущий угол поворота (обратная связь)
    4.  **Управляющая схема** — сравнивает заданный угол с текущим и корректирует
    5.  **Вал** — то, что мы видим и чем управляем

*   **Подключение (3 провода):**
    - **Коричневый/чёрный** → `GND` (земля)
    - **Красный** → `5V` (питание)
    - **Жёлтый/оранжевый** → **сигнальный пин** на Arduino (например, 9)

### **3. Принцип управления: ШИМ-сигналы**
Сервопривод управляется не напряжением, а **длительностью импульсов**. Arduino посылает короткие импульсы каждые 20 миллисекунд (50 раз в секунду). Длительность импульса определяет угол:

- **0.5 мс (500 микросекунд)** → **0°** (крайнее левое положение)
- **1.5 мс (1500 микросекунд)** → **90°** (центральное положение)
- **2.5 мс (2500 микросекунд)** → **180°** (крайнее правое положение)

![ШИМ для сервопривода](https://www.arduino.cc/en/uploads/Tutorial/sweep.gif)

**Важно:** Большинство сервоприводов SG90 работают в диапазоне **0-180°**, но бывают сервоприводы на 270° и 360°.

### **4. Библиотека Servo.h — магия простоты**
Вместо того чтобы вручную генерировать ШИМ-сигналы нужной длительности, мы будем использовать **встроенную библиотеку** `Servo.h`. Она берёт всю сложную работу на себя!

Что умеет библиотека:
1.  Автоматически генерировать правильные ШИМ-сигналы
2.  Управлять сразу несколькими сервоприводами (до 12 на Arduino Uno)
3.  Обеспечивать плавное движение
4.  Работать на любом цифровом пине (не только на ШИМ-пинах!)

### **5. Особенности питания сервопривода**
Сервопривод при движении потребляет **большой ток** (до 500-700 мА при нагрузке).
- **При подключении одного сервопривода** можно питать его от пина `5V` Arduino.
- **При подключении нескольких сервоприводов или при работе под нагрузкой** обязательно используйте **внешний источник питания 5V** с отдельным подключением к макетной плате!

🔗 **Полезные ресурсы:**
*   **Официальная документация по библиотеке Servo:** https://www.arduino.cc/reference/en/libraries/servo/
*   **Подробный гайд по сервоприводам:** https://alexgyver.ru/lessons/servo/

---

## **🛠 Практическая часть: Создаём "Умную руку" с плавным управлением**

**📦 Что нужно из коробки:**
1.  Плата **Arduino Uno**.
2.  **Макетная плата**.
3.  **Сервопривод SG90** (180°).
4.  **Потенциометр 10 кОм**.
5.  **Светодиод и резистор 220 Ом** (для индикации крайних положений).
6.  **Провода «папа-папа»**.
7.  **Провода «папа-мама»** (3 штуки, для удобного подключения сервопривода).

### **🔌 Шаг 1: Собираем схему "Сервопривод с ручным управлением"**
Собираем систему: потенциометр как аналоговый вход для задания угла, сервопривод как выход, светодиод как индикатор.

![Схема сервопривода с потенциометром](https://www.arduino.cc/wiki/static/9c1e8c8e8e9d8e9c8e9d8e9c8e9d8e9c/29114/circuit.png)

**Алгоритм сборки:**
1.  **Подключи сервопривод:**
    - **Коричневый провод (GND)** → `GND` на макетной плате
    - **Красный провод (VCC)** → `5V` на макетной плате  
    - **Жёлтый провод (SIGNAL)** → **цифровой пин 9** на Arduino

2.  **Подключи потенциометр** (как на занятии 3):
    - Левый вывод → `GND`
    - Правый вывод → `5V`
    - Средний вывод → **аналоговый пин A0**

3.  **Подключи светодиод-индикатор:**
    - Длинная ножка (+) → пин 13 через резистор 220 Ом
    - Короткая ножка (-) → `GND`

4.  **Не забудь соединить макетные рельсы `5V` и `GND` с соответствующими пинами Arduino!**

**Проверь себя по чек-листу:**
- [ ] Сервопривод подключён правильно (коричневый → GND, красный → 5V, жёлтый → 9)
- [ ] Потенциометр подключён тремя проводами
- [ ] Светодиод подключён через резистор
- [ ] Все GND соединены между собой

### **💻 Шаг 2: Пишем код "Сервопривод с плавным управлением"**
Загрузи этот код. Поворачивая ручку потенциометра, ты будешь плавно управлять углом поворота сервопривода. При достижении крайних положений (0° или 180°) будет загораться светодиод.

```cpp
/*
   Занятие 5. Умная рука с плавным управлением.
   Угол сервопривода плавно регулируется потенциометром.
   Светодиод загорается при достижении крайних положений.
*/

// Подключаем библиотеку для работы с сервоприводом
#include <Servo.h>

// Константы с именами пинов
const int POT_PIN = A0;      // Потенциометр на A0
const int SERVO_PIN = 9;     // Сервопривод на пине 9
const int LED_PIN = 13;      // Светодиод-индикатор на пине 13

// Создаём объект для управления сервоприводом
// Можно создать несколько объектов для нескольких сервоприводов
Servo myServo;

// Переменные
int potValue = 0;           // Сырое значение с потенциометра (0-1023)
int angle = 0;              // Вычисленный угол для сервопривода (0-180)
int lastAngle = 0;          // Предыдущий угол (для обнаружения изменений)

void setup() {
  // Инициализируем последовательный порт для отладки
  Serial.begin(9600);
  Serial.println("Система управления сервоприводом готова!");
  Serial.println("Поворачивайте потенциометр.");
  
  // Привязываем объект myServo к конкретному пину
  myServo.attach(SERVO_PIN);
  
  // Настраиваем пин светодиода как выход
  pinMode(LED_PIN, OUTPUT);
  
  // Устанавливаем сервопривод в начальное положение (90°)
  myServo.write(90);
  delay(500); // Даём время сервоприводу повернуться
}

void loop() {
  // ШАГ 1: Считываем значение с потенциометра
  potValue = analogRead(POT_PIN);
  
  // ШАГ 2: Преобразуем диапазон 0-1023 в диапазон 0-180 градусов
  // map(значение, отНиж, отВерх, кНиж, кВерх)
  angle = map(potValue, 0, 1023, 0, 180);
  
  // ШАГ 3: Устанавливаем сервоприводу новый угол
  // (только если угол изменился, чтобы не дергать сервопривод без необходимости)
  if (angle != lastAngle) {
    myServo.write(angle);
    lastAngle = angle;
    
    // Выводим информацию в монитор порта
    Serial.print("Потенциометр: ");
    Serial.print(potValue);
    Serial.print(" | Угол: ");
    Serial.print(angle);
    Serial.println("°");
  }
  
  // ШАГ 4: Управляем светодиодом-индикатором
  // Если сервопривод в крайних положениях (0° или 180°) - зажигаем светодиод
  if (angle == 0 || angle == 180) {
    digitalWrite(LED_PIN, HIGH);
    Serial.println("КРАЙНЕЕ ПОЛОЖЕНИЕ!");
  } else {
    digitalWrite(LED_PIN, LOW);
  }
  
  // Небольшая задержка для стабильности
  delay(20);
}
```

**🔍 Что делать после загрузки кода:**
1.  Открой **Инструменты -> Монитор порта** (Ctrl+Shift+M), скорость **9600**.
2.  **Медленно поворачивай потенциометр** и наблюдай:
    - Как сервопривод плавно поворачивается следом за ручкой
    - Как в мониторе порта отображаются значения потенциометра и угла
    - Как светодиод загорается при достижении крайних положений (0° и 180°)
3.  **Попробуй резко повернуть потенциометр** — посмотри, как сервопривод реагирует на резкие изменения.

**🎉 Отлично! Ты создал свою первую механическую систему с точным позиционированием!**

---

## **🧠 Разбираем код по косточкам**

1.  **`#include <Servo.h>`**
    - Директива **подключения библиотеки**. Библиотека — это набор готовых функций для работы с конкретным устройством.
    - Библиотека `Servo.h` уже установлена в Arduino IDE по умолчанию.

2.  **`Servo myServo;`**
    - Создание **объекта** типа `Servo`. Объект — это "умная" переменная, которая содержит не только данные, но и функции для работы с сервоприводом.
    - Имя `myServo` можно заменить на любое другое (например, `servo1`, `armServo`).

3.  **`myServo.attach(SERVO_PIN);`**
    - **Привязка** объекта к конкретному пину Arduino.
    - Важно: эту функцию вызываем только один раз в `setup()`.

4.  **`myServo.write(angle);`**
    - Основная функция управления. Устанавливает сервоприводу угол от 0 до 180 градусов.
    - Библиотека автоматически генерирует правильные ШИМ-сигналы.

5.  **Проверка изменения угла:**
    ```cpp
    if (angle != lastAngle) {
      myServo.write(angle);
      lastAngle = angle;
    }
    ```
    - **Оптимизация:** Отправляем команду сервоприводу только если угол действительно изменился.
    - Экономит ресурсы и уменьшает износ сервопривода.

6.  **Работа со светодиодом:**
    ```cpp
    if (angle == 0 || angle == 180) {
      digitalWrite(LED_PIN, HIGH);
    }
    ```
    - Логический оператор **`||`** означает "ИЛИ".
    - Светодиод загорится, если угол равен 0 **ИЛИ** равен 180.

---

## **🚀 Проекты для самостоятельной работы**

### **🔹 Проект 5.1: "Автоматический сканер"**
Заставь сервопривод автоматически сканировать пространство, как радар.
*   **Логика:** Сервопривод плавно поворачивается от 0° до 180°, затем обратно от 180° до 0°.
*   **Дополнение:** Добавь **ультразвуковой дальномер HC-SR04** (если он есть) на вал сервопривода. При сканировании выводи в монитор порта угол и расстояние до объекта.
*   **Усложнение:** При обнаружении объекта на расстоянии менее 20 см сервопривод останавливается и включает светодиод.

### **🔹 Проект 5.2: "Робот-манипулятор с двумя степенями свободы"**
Собери простейший манипулятор из **двух сервоприводов**.
*   **Что нужно:** 2 сервопривода, 2 потенциометра.
*   **Сборка:** Закрепи один сервопривод горизонтально (основание), на его вал прикрепи второй сервопривод вертикально (рука). Для крепления можно использовать картон, пластик или конструктор.
*   **Логика:** Первый потенциометр управляет поворотом основания (0-180°), второй — подъёмом/опусканием руки (30-150°, чтобы не сломать конструкцию).
*   **Супер-задание:** Добавь кнопку для записи положений в память и воспроизведения запрограммированного движения.

### **🔹 Проект 5.3: "Серво-тестер и калибратор"**
Создай устройство для тестирования и калибровки сервоприводов.
*   **Логика:** Используй **три кнопки**:
    - Кнопка 1: Увеличить угол на 1°
    - Кнопка 2: Уменьшить угол на 1°
    - Кнопка 3: Автоматический прогон по всем положениям (0° → 90° → 180° → 90° → 0°)
*   **Дополнение:** Добавь **7-сегментный индикатор** или **LCD-дисплей** для отображения текущего угла.
*   **Практическая польза:** Таким тестером можно проверять рабочий диапазон и плавность хода любых сервоприводов.

---

## **❓ Вопросы на засыпку**

1.  Объясни своими словами: почему для управления сервоприводом используется не просто напряжение, а именно **длительность импульсов**? В чём преимущество такого подхода?
2.  Что произойдёт, если попытаться установить сервоприводу угол 200° (больше 180)? А если -10°? Проверь на практике.
3.  Почему в коде мы сравниваем `angle != lastAngle` перед отправкой команды сервоприводу? Что плохого в том, чтобы отправлять команду `myServo.write(angle)` на каждом цикле `loop()`?
4.  Представь, что тебе нужно сделать плавное движение сервопривода из точки А в точку Б за 3 секунды. Как бы ты это реализовал? Опиши алгоритм.
5.  Придумай **три оригинальных применения** для сервопривода в школьном проекте (не робот и не манипулятор).

---

## **📌 Критерии успеха: как понять, что ты молодец?**

*   ✅ Сервопривод **плавно и точно** следует за поворотом потенциометра.
*   ✅ Светодиод **чётко загорается** при достижении крайних положений.
*   ✅ В **Мониторе порта** корректно отображаются значения.
*   ✅ Понимаешь разницу между `#include`, созданием объекта и вызовом методов (`attach()`, `write()`).
*   ✅ **Самостоятельно** реализовал один из проектов и можешь объяснить его работу.

---

## **🆘 Скорая помощь: если что-то пошло не так...**

*   **Сервопривод не двигается, только дрожит или пищит:**
    - **Недостаточное питание!** Самая частая проблема. Подключи сервопривод к **отдельному источнику 5V** (например, powerbank) или используй блок питания.
    - Проверь, что красный провод подключён именно к `5V`, а не к `3.3V`.
*   **Сервопривод поворачивается только в одну сторону или не до конца:**
    - Возможно, сервопривод **нуждается в калибровке**. Некоторые сервоприводы имеют немного другой диапазон (например, 10-170° вместо 0-180°).
    - Проверь формулу `map()` — возможно, нужно скорректировать диапазоны.
*   **Сервопривод сильно греется даже без нагрузки:**
    - **Механическая блокировка!** Не давай сервоприводу упираться в препятствия. Если вал не может повернуться на заданный угол, мотор будет пытаться это сделать и перегреется.
    - Всегда оставляй небольшой запас по углу (например, 5-175° вместо 0-180°).
*   **Arduino перезагружается при движении сервопривода:**
    - Это признак **просадки напряжения**. Сервопривод "тянет" слишком много тока, и Arduino не хватает питания.
    - Решение: внешний источник питания для сервопривода с общим GND с Arduino.

**Профессиональный совет:** При работе с сервоприводами **всегда заземляй (GND) все компоненты системы** — Arduino, сервоприводы, внешние блоки питания. Разность потенциалов в "землях" — частая причина странного поведения. И помни: сервоприводы под нагрузкой потребляют много тока — рассчитывай питание заранее!
