Занятие 5: Ультразвуковой дальномер HC-SR04

# Углублённое руководство: Ультразвуковой дальномер HC-SR04

## 🎯 Цель занятия
**Основная цель:** Научиться измерять расстояние до объектов с помощью ультразвукового датчика HC-SR04 и интегрировать эти данные в различные электронные системы.

**Детализация целей:**
1. Понимать физические принципы работы ультразвуковых датчиков
2. Освоить подключение и калибровку HC-SR04
3. Научиться обрабатывать и интерпретировать полученные данные
4. Применять полученные знания для создания полезных устройств

---

## 📖 Углублённая теоретическая часть

### 1. Ультразвук: Физика явления

**Что такое звук?**
- Механические колебания частиц среды (воздуха, воды, твердых тел)
- Распространяются в виде продольных волн
- Характеризуются:
  - **Частотой** (Гц) - количество колебаний в секунду
  - **Длиной волны** (λ = v/f)
  - **Скоростью распространения** (зависит от среды)

**Человеческий слух:**
- Диапазон: 20 Гц - 20 кГц
- Ультразвук: >20 кГц (не слышим человеком)
- Инфразвук: <20 Гц (не слышим человеком)

**Скорость звука в разных средах:**
```
Воздух (20°C)     : 343 м/с ≈ 0.0343 см/мкс
Вода (пресная)    : 1480 м/с
Сталь             : 5100 м/с
Резина            : 1600 м/с
```

**Факторы, влияющие на скорость звука в воздухе:**
```
Температура: v = 331.4 + 0.6 * T(°C) м/с
Влажность: увеличение на 0.1-0.3% при 100% влажности
Атмосферное давление: незначительное влияние
```

### 2. Принцип работы HC-SR04

**Технические характеристики:**
```
Рабочее напряжение    : 5V DC
Рабочий ток          : 15 мА
Рабочая частота      : 40 кГц
Измеряемое расстояние: 2 см - 400 см
Точность             : ±3 мм
Угол измерения       : 15°
```

**Подробный принцип эхолокации:**
1. **Генерация импульса**: При подаче импульса на Trig (минимум 10 мкс) генератор создает 8 импульсов ультразвука частотой 40 кГц
2. **Распространение**: Импульс распространяется в воздухе со скоростью звука
3. **Отражение**: При встрече с препятствием часть энергии отражается
4. **Прием**: Отраженный сигнал попадает на приемный пьезоэлемент
5. **Обработка**: Внутренняя схема формирует импульс на выводе Echo, длительность которого пропорциональна времени прохождения сигнала

**Математическая модель:**
```
t = время между отправкой и приемом (мкс)
S = расстояние до объекта (см)

S = (t * v) / 2
где v = 0.0343 см/мкс (при 20°C)

Для температуры T: v = (331.4 + 0.6 * T) / 10000 см/мкс
```

### 3. Детальная распиновка и внутреннее устройство

**Распиновка HC-SR04:**
```
1. VCC  : +5V (диапазон 4.5V - 5.5V)
2. Trig : Input TTL (3.3V или 5V логика)
3. Echo : Output TTL (5V уровень!)
4. GND  : Земля
```


**Схема делителя напряжения для 3.3V систем:**
```
Echo HC-SR04 → R1 (1kΩ) → Pin микроконтроллера
                    ↓
                   R2 (2kΩ)
                    ↓
                   GND
```

**Внутренняя структура датчика:**
```
            ┌─────────────────┐
            │   Управляющая   │
Trig ──────►│    логика       │─────► Пьезоизлучатель
            │   (таймеры)     │
            │                 │
Микрофон ──►│  Усилитель +    │
            │  компаратор     │─────► Echo
            └─────────────────┘
```

### 4. Погрешности измерения и ограничения

**Основные источники погрешности:**
1. **Температурная зависимость** - главный фактор
   - Решение: использовать датчик температуры (DHT11, DS18B20) для коррекции
   
2. **Угол наклона объекта**
   - Максимальная точность: при перпендикулярном расположении
   - При угле >15° сигнал может не вернуться

3. **Характеристики поверхности:**
   - **Хорошо отражают**: твердые, плоские, гладкие поверхности (металл, стекло)
   - **Плохо отражают**: мягкие, пористые, шероховатые поверхности (ткань, поролон)
   - **Поглощают**: пушистые, ворсистые материалы

4. **Множественные отражения** (эхо-сигналы от нескольких объектов)

5. **Акустические помехи** (другие источники ультразвука)

**Мертвая зона:**
- Минимальное расстояние: 2 см
- Причина: время переключения между передачей и приемом
- Не измеряйте объекты ближе 2 см!

### 5. Альтернативные ультразвуковые датчики

**Сравнение популярных моделей:**
```
HC-SR04    : 2-400 см, 40 кГц, 15°, дешевый
HC-SR05    : 2-450 см, 40 кГц, 15°, более точный
JSN-SR04T  : 25-600 см, водонепроницаемый
US-100    : 2-450 см, встроенная термокомпенсация
MaxSonar  : 0-500 см, аналоговый/цифровой выход
```

---

## 🔌 Детальная схема подключения

### Базовое подключение к Arduino Uno:

**Компоненты:**
- Arduino Uno
- HC-SR04
- Макетная плата 400 точек
- Провода "папа-папа" - 6 шт
- Источник питания 5V (если требуется)

**Пошаговое подключение:**

```
Шаг 1: Питание датчика
HC-SR04 VCC → 5V на Arduino
HC-SR04 GND → GND на Arduino

Шаг 2: Сигнальные провода
HC-SR04 Trig → Цифровой пин 9 (Arduino)
HC-SR04 Echo → Цифровой пин 10 (Arduino)

Шаг 3: Дополнительный светодиод
LED анод → Резистор 220Ω → Пин 13
LED катод → GND

Шаг 4: Проверка подключения
1. Питание 5V → проверьте светодиод на датчике
2. GND → общая земля для всех компонентов
```

**Важные нюансы:**
1. **Длина проводов**: не более 50 см для сигнальных линий
2. **Помехи**: избегайте прокладки проводов рядом с силовыми линиями
3. **Фильтрация по питанию**: добавьте конденсатор 100 мкФ между VCC и GND датчика
4. **Подтягивающий резистор**: для стабильности можно добавить резистор 10кОм между Echo и +5V

---

## 💻 Углубленный анализ кода

### Полная программа с комментариями:

```cpp
/*
 * Программа для ультразвукового дальномера HC-SR04
 * с термокомпенсацией и фильтрацией данных
 * Автор: [Ваше имя]
 * Дата: [Дата]
 */

// =========== КОНСТАНТЫ И НАСТРОЙКИ ===========

// Пины подключения
const int TRIG_PIN = 9;      // Пин для запуска измерения
const int ECHO_PIN = 10;     // Пин для приема сигнала
const int LED_PIN = 13;      // Пин светодиода индикации

// Физические константы
const float SPEED_OF_SOUND_20C = 0.0343; // см/мкс при 20°C
const float TEMP_COEFFICIENT = 0.0006;   // Коэффициент температурной поправки

// Настройки измерения
const int MEASUREMENT_DELAY = 50;        // Задержка между измерениями (мс)
const int NUM_SAMPLES = 5;               // Количество усредняемых измерений
const int MIN_DISTANCE = 2;              // Минимальное расстояние (см)
const int MAX_DISTANCE = 400;            // Максимальное расстояние (см)

// Параметры фильтра
const float ALPHA = 0.3;                 // Коэффициент экспоненциального фильтра (0-1)

// =========== ГЛОБАЛЬНЫЕ ПЕРЕМЕННЫЕ ===========
float currentTemperature = 20.0;         // Текущая температура (для коррекции)
float filteredDistance = 0.0;            // Отфильтрованное расстояние

// =========== ФУНКЦИЯ НАСТРОЙКИ ===========
void setup() {
  // Инициализация последовательного порта
  Serial.begin(115200);                  // Более высокая скорость для плавного вывода
  Serial.println(F("Ультразвуковой дальномер HC-SR04"));
  Serial.println(F("=================================="));
  
  // Настройка режимов пинов
  pinMode(TRIG_PIN, OUTPUT);
  pinMode(ECHO_PIN, INPUT);
  pinMode(LED_PIN, OUTPUT);
  
  // Инициализация
  digitalWrite(TRIG_PIN, LOW);
  digitalWrite(LED_PIN, LOW);
  
  delay(100);  // Даем время на стабилизацию датчика
  
  Serial.println(F("Система готова к работе"));
  Serial.println();
}

// =========== ОСНОВНОЙ ЦИКЛ ===========
void loop() {
  // 1. Измерение расстояния с усреднением
  float distance = getAverageDistance(NUM_SAMPLES);
  
  // 2. Фильтрация данных (экспоненциальное скользящее среднее)
  filteredDistance = exponentialFilter(filteredDistance, distance, ALPHA);
  
  // 3. Проверка на корректность данных
  if (isValidDistance(filteredDistance)) {
    // 4. Вывод результатов
    printDistanceInfo(filteredDistance);
    
    // 5. Визуальная индикация
    visualIndication(filteredDistance);
  } else {
    Serial.println(F("Ошибка измерения!"));
    digitalWrite(LED_PIN, HIGH);  // Индикация ошибки
    delay(200);
    digitalWrite(LED_PIN, LOW);
  }
  
  // 6. Пауза между измерениями
  delay(MEASUREMENT_DELAY);
}

// =========== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ===========

/**
 * Функция для одиночного измерения расстояния
 * @return расстояние в сантиметрах
 */
float measureDistance() {
  // Гарантируем стабильный низкий уровень на Trig
  digitalWrite(TRIG_PIN, LOW);
  delayMicroseconds(2);
  
  // Формируем импульс 10 мкс
  digitalWrite(TRIG_PIN, HIGH);
  delayMicroseconds(10);
  digitalWrite(TRIG_PIN, LOW);
  
  // Измеряем длительность импульса на Echo
  // pulseIn возвращает 0 при таймауте (1 секунда по умолчанию)
  unsigned long duration = pulseIn(ECHO_PIN, HIGH, 30000); // Таймаут 30 мс
  
  // Рассчитываем расстояние с температурной компенсацией
  if (duration > 0) {
    // Корректировка скорости звука в зависимости от температуры
    float speedOfSound = SPEED_OF_SOUND_20C + 
                        (currentTemperature - 20.0) * TEMP_COEFFICIENT;
    
    // Расчет расстояния: (время * скорость) / 2
    float distance = duration * speedOfSound / 2.0;
    
    return distance;
  }
  
  return 0.0;  // Ошибка измерения
}

/**
 * Усреднение нескольких измерений
 * @param samples количество измерений для усреднения
 * @return усредненное расстояние
 */
float getAverageDistance(int samples) {
  float sum = 0;
  int validSamples = 0;
  
  for (int i = 0; i < samples; i++) {
    float distance = measureDistance();
    
    if (isValidDistance(distance)) {
      sum += distance;
      validSamples++;
    }
    
    delay(10);  // Небольшая пауза между измерениями
  }
  
  if (validSamples > 0) {
    return sum / validSamples;
  }
  
  return 0.0;  // Нет валидных измерений
}

/**
 * Экспоненциальный фильтр (EMA)
 * @param oldValue предыдущее отфильтрованное значение
 * @param newValue новое измерение
 * @param alpha коэффициент фильтрации (0-1)
 * @return отфильтрованное значение
 */
float exponentialFilter(float oldValue, float newValue, float alpha) {
  return alpha * newValue + (1 - alpha) * oldValue;
}

/**
 * Проверка корректности расстояния
 * @param distance проверяемое расстояние
 * @return true если расстояние корректно
 */
bool isValidDistance(float distance) {
  return (distance >= MIN_DISTANCE && distance <= MAX_DISTANCE);
}

/**
 * Вывод информации о расстоянии
 * @param distance расстояние для вывода
 */
void printDistanceInfo(float distance) {
  static unsigned long lastPrintTime = 0;
  
  // Ограничиваем частоту вывода в Serial
  if (millis() - lastPrintTime > 200) {
    Serial.print(F("Расстояние: "));
    Serial.print(distance, 1);  // 1 знак после запятой
    Serial.print(F(" см | "));
    
    // Перевод в дюймы
    Serial.print(distance / 2.54, 1);
    Serial.print(F(" дюймов | "));
    
    // Графическое представление
    int bars = map(constrain(distance, 0, 100), 0, 100, 0, 20);
    for (int i = 0; i < bars; i++) {
      Serial.print("█");
    }
    
    Serial.println();
    lastPrintTime = millis();
  }
}

/**
 * Визуальная индикация в зависимости от расстояния
 * @param distance текущее расстояние
 */
void visualIndication(float distance) {
  if (distance < 10) {
    // Очень близко: быстрое мигание
    digitalWrite(LED_PIN, HIGH);
    delay(50);
    digitalWrite(LED_PIN, LOW);
  } else if (distance < 30) {
    // Близко: постоянное свечение
    digitalWrite(LED_PIN, HIGH);
  } else if (distance < 50) {
    // Средняя дистанция: медленное мигание
    digitalWrite(LED_PIN, HIGH);
    delay(200);
    digitalWrite(LED_PIN, LOW);
  } else {
    // Далеко: светодиод выключен
    digitalWrite(LED_PIN, LOW);
  }
}

/**
 * Функция для ручной калибровки (вызывается по команде)
 */
void calibrate() {
  Serial.println(F("=== КАЛИБРОВКА ==="));
  Serial.println(F("1. Установите объект на известном расстоянии"));
  Serial.println(F("2. Введите расстояние в см"));
  
  while (!Serial.available()) {
    // Ждем ввода
  }
  
  float knownDistance = Serial.parseFloat();
  Serial.print(F("Калибровка для: "));
  Serial.print(knownDistance);
  Serial.println(F(" см"));
  
  // Измеряем текущее значение
  float measuredDistance = getAverageDistance(10);
  float correctionFactor = knownDistance / measuredDistance;
  
  Serial.print(F("Коэффициент коррекции: "));
  Serial.println(correctionFactor, 4);
  Serial.println(F("Калибровка завершена"));
}
```

---

## 🎓 Углубленные темы для изучения

### 1. Тригонометрия для ультразвуковых измерений

**Измерение высоты объекта:**
```cpp
// При известном расстоянии L и угле наклона α
// Высота h = L * sin(α)

float calculateHeight(float distance, float angleDegrees) {
  float angleRadians = angleDegrees * PI / 180.0;
  return distance * sin(angleRadians);
}
```

### 2. Устранение "шума" и случайных выбросов

**Медианный фильтр:**
```cpp
float medianFilter(float newValue) {
  static float buffer[5] = {0};
  static byte index = 0;
  
  // Добавляем новое значение в буфер
  buffer[index] = newValue;
  index = (index + 1) % 5;
  
  // Сортировка пузырьком для нахождения медианы
  float tempBuffer[5];
  memcpy(tempBuffer, buffer, sizeof(buffer));
  
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      if (tempBuffer[i] > tempBuffer[j]) {
        float temp = tempBuffer[i];
        tempBuffer[i] = tempBuffer[j];
        tempBuffer[j] = temp;
      }
    }
  }
  
  // Возвращаем медиану (средний элемент)
  return tempBuffer[2];
}
```

### 3. Определение скорости движения объекта

```cpp
class SpeedCalculator {
private:
  float prevDistance = 0;
  unsigned long prevTime = 0;
  
public:
  float calculateSpeed(float currentDistance) {
    unsigned long currentTime = millis();
    float speed = 0;
    
    if (prevTime > 0) {
      float distanceDelta = currentDistance - prevDistance;
      float timeDelta = (currentTime - prevTime) / 1000.0; // в секундах
      
      // Скорость = изменение расстояния / изменение времени
      // Учитываем направление (приближение/удаление)
      speed = abs(distanceDelta) / timeDelta;
    }
    
    prevDistance = currentDistance;
    prevTime = currentTime;
    
    return speed; // см/сек
  }
};
```

---

## 📚 Дополнительные ресурсы и ссылки

### Книги и учебные материалы:
1. **"Arduino для начинающих"** - Джереми Блум
   - Глава 7: Датчики расстояния и движения

2. **"Физика. Полный курс"** - Орир
   - Глава о звуковых и ультразвуковых волнах

3. **Курс на Coursera** - "The Arduino Platform and C Programming"
   - Модуль: Sensor Integration

### Онлайн ресурсы:
1. **Официальная документация:**
   - [Datasheet HC-SR04](https://cdn.sparkfun.com/datasheets/Sensors/Proximity/HCSR04.pdf)
   - [Arduino Reference - pulseIn()](https://www.arduino.cc/reference/en/language/functions/advanced-io/pulsein/)

2. **Образовательные видео:**
   - [How Ultrasound Works](https://www.youtube.com/watch?v=QkG4v-ncT48)
   - [HC-SR04 Deep Dive](https://www.youtube.com/watch?v=6F1B_N6LuKw)

3. **Симуляторы и онлайн-IDE:**
   - [Wokwi Simulator](https://wokwi.com/) - симуляция Arduino проектов
   - [Tinkercad Circuits](https://www.tinkercad.com/circuits) - виртуальные схемы

4. **Форумы и сообщества:**
   - [Arduino Forum](https://forum.arduino.cc/)
   - [StackExchange - Electrical Engineering](https://electronics.stackexchange.com/)
   - [Reddit r/arduino](https://www.reddit.com/r/arduino/)

### Научные статьи и исследования:
1. **"Accuracy Analysis of Ultrasonic Sensors"** - IEEE Transactions
   - DOI: 10.1109/JSEN.2018.2794582

2. **"Temperature Compensation in Ultrasonic Distance Measurement"**
   - International Journal of Engineering Research

---

## 🧪 Расширенные проекты и исследования

### Проект 1: Трехмерное сканирование пространства

**Идея:** Создать систему, которая строит 3D-карту помещения
**Компоненты:**
- HC-SR04 на сервоприводе (2 оси)
- Шаговый двигатель для вращения
- SD-карта для сохранения данных
- OLED дисплей для отображения

**Алгоритм:**
1. Сканирование по горизонтали (0-180°)
2. Сканирование по вертикали (0-90°)
3. Преобразование полярных координат в декартовы
4. Визуализация в Processing/OpenGL

### Проект 2: Система подсчета людей

**Идея:** Определение количества людей в комнате
**Метод:** Измерение изменений расстояния на разных высотах
**Логика:**
- Датчик на высоте 50 см: детектирование ног
- Датчик на высоте 150 см: детектирование туловища
- Корреляция сигналов двух датчиков

### Проект 3: Акустическая томография

**Идея:** Определение плотности объектов по отраженному сигналу
**Метод:** Анализ амплитуды отраженного сигнала
**Применение:** Обнаружение пустот в стенах, контроль качества материалов

---

## ❓ Вопросы для глубокого понимания

### Уровень 1: Базовый
1. Почему ультразвук не слышен человеку?
2. Какие факторы влияют на точность измерений?
3. Почему минимальное расстояние 2 см?

### Уровень 2: Продвинутый
4. Как работает функция pulseIn() на аппаратном уровне?
5. Почему при измерении расстояния в воде нужна другая формула?
6. Как можно измерить расстояние меньше 2 см с помощью HC-SR04?

### Уровень 3: Экспертный
7. Разработайте алгоритм компенсации температуры без дополнительных датчиков
8. Как определить материал объекта по характеристикам отраженного сигнала?
9. Предложите метод увеличения максимального расстояния измерения

---

## 🔧 Расширенные техники программирования

### 1. Использование прерываний для точного измерения:
```cpp
volatile unsigned long pulseStart = 0;
volatile unsigned long pulseEnd = 0;
volatile bool pulseReceived = false;

void setup() {
  attachInterrupt(digitalPinToInterrupt(ECHO_PIN), echoISR, CHANGE);
}

void echoISR() {
  if (digitalRead(ECHO_PIN) == HIGH) {
    pulseStart = micros();
  } else {
    pulseEnd = micros();
    pulseReceived = true;
  }
}
```

### 2. Калибровка датчика с сохранением в EEPROM:
```cpp
#include <EEPROM.h>

struct CalibrationData {
  float offset;
  float scaleFactor;
  int checksum;
};

void saveCalibration(float offset, float scale) {
  CalibrationData data;
  data.offset = offset;
  data.scaleFactor = scale;
  data.checksum = calculateChecksum(data);
  
  EEPROM.put(0, data);
}
```

### 3. Многозадачность с помощью millis():
```cpp
class UltrasonicSensor {
private:
  unsigned long lastMeasurement = 0;
  const unsigned long interval = 100;
  
public:
  void update() {
    unsigned long currentMillis = millis();
    
    if (currentMillis - lastMeasurement >= interval) {
      performMeasurement();
      lastMeasurement = currentMillis;
    }
  }
};
```

---

## 🏆 Критерии оценки проектов

### Базовый уровень (3/5):
- [ ] Датчик корректно подключен
- [ ] Вывод данных в Serial Monitor
- [ ] Работает индикация светодиодом

### Средний уровень (4/5):
- [ ] Фильтрация данных (усреднение)
- [ ] Обработка ошибок измерения
- [ ] Реализована температурная компенсация
- [ ] Проект имеет практическое применение

### Продвинутый уровень (5/5):
- [ ] Использование прерываний для измерения
- [ ] Калибровка с сохранением параметров
- [ ] Расширенная визуализация данных
- [ ] Интеграция с другими устройствами (Bluetooth, Wi-Fi)
- [ ] Документация к проекту и код с комментариями

---

## 🆘 Расширенное устранение неисправностей

### Проблема: Неточные измерения
**Возможные причины:**
1. Нестабильное питание
2. Акустические помехи
3. Неправильная ориентация датчика

**Решения:**
```
1. Добавить конденсатор 100 мкФ к питанию датчика
2. Установить звукопоглощающий материал вокруг датчика
3. Проверить перпендикулярность к измеряемому объекту
```


