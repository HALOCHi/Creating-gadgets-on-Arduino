# **Занятие 4: Мир датчиков — измерение температуры и создание термостата**

---

## **🎯 Цель занятия:**
Научить Arduino "чувствовать" окружающую среду! Сегодня ты превратишь свою плату в цифровой термометр и создашь первое **устройство с обратной связью** — простой термостат, который будет принимать решения на основе измерений. Ты освоишь работу с аналоговыми датчиками, научишься преобразовывать "сырые" значения в понятные физические величины и создашь систему, которая реагирует на изменения в мире.

---

## **📖 Теоретическая часть: Как Arduino "чувствует" температуру**

### **1. От потенциометра к датчику: мир аналоговых сенсоров**
На прошлом занятии мы управляли сигналом вручную с помощью потенциометра. Но в реальных устройствах значения должны приходить **от окружающей среды** — это работа **датчиков**.

*   **Датчик** — это устройство, которое преобразует физическую величину (температуру, освещённость, давление) в электрический сигнал (обычно напряжение), который может понять Arduino.
*   **Аналоговые датчики** (как наш TMP36) выдают плавно меняющееся напряжение, пропорциональное измеряемой величине.

### **2. Датчик температуры TMP36: принцип работы**
TMP36 — точный, недорогой и простой в использовании датчик. Его работа основана на свойстве полупроводников менять свои характеристики с температурой.

![TMP36 Pinout](https://cdn-learn.adafruit.com/assets/assets/000/001/533/medium800/sensors_pinout.jpg)

*   **Распиновка (ноги):**
    *   **Левый (если плоской стороной к себе) — VCC:** Питание (+2.7V до +5.5V). Мы подадим **+5V**.
    *   **Средний — Vout:** Аналоговый выход. Выдаёт напряжение, пропорциональное температуре.
    *   **Правый — GND:** Земля.

### **3. Как преобразовать напряжение в градусы: формула и логика**
Датчик выдаёт **10 мВ на каждый градус Цельсия** с некоторым смещением.

**Формула преобразования:**
1.  Считываем "сырое" значение с аналогового пина: `sensorValue = analogRead(A0);` (0-1023)
2.  Преобразуем в напряжение (Вольты):  
    `voltage = sensorValue * (5.0 / 1023.0);`  
    (5.0 — опорное напряжение, 1023.0 — максимальное значение АЦП)
3.  Преобразуем напряжение в температуру (°C) для TMP36:  
    `temperature = (voltage - 0.5) * 100;`  
    Почему? При 0°C датчик выдаёт 0.5V. Каждый градус добавляет 0.01V (10 мВ).

**Важно:** TMP36 выдаёт напряжение, линейно зависящее от температуры. У других датчиков формулы могут быть сложнее.

### **4. Концепция обратной связи и пороговое управление**
Сегодня мы создадим **систему с обратной связью** — устройство, которое:
1.  **Измеряет** параметр (температуру)
2.  **Сравнивает** с заданным значением (порогом)
3.  **Принимает решение** и **воздействует** на систему (включает/выключает обогрев или охлаждение)

Это основа всех систем автоматики: термостаты, климат-контроль, системы безопасности.

### **5. Новые функции: float и математические операции**
Для точных вычислений с дробными числами нам понадобится новый тип данных:
*   **`int`** — целые числа (например, 25, -3, 1023)
*   **`float`** — числа с плавающей точкой, дробные (например, 25.75, 3.14, 2.5)

При работе с `float` нужно:
1.  Использовать точки вместо запятых: `3.14`, а не `3,14`
2.  Указывать дробную часть у констант: `5.0` вместо `5`
3.  Помнить, что операции с `float` занимают больше памяти и времени

🔗 **Полезные ресурсы:**
*   **Документация по TMP36:** https://cdn-learn.adafruit.com/downloads/pdf/tmp36-temperature-sensor.pdf
*   **Руководство по датчикам температуры:** https://alexgyver.ru/lessons/temperature-sensors/

---

## **🛠 Практическая часть: Создаём цифровой термометр с сигнализацией**

**📦 Что нужно из коробки:**
1.  Плата **Arduino Uno**.
2.  **Макетная плата**.
3.  **Датчик температуры TMP36** (или аналогичный аналоговый датчик).
4.  **Светодиод красный** (для сигнала перегрева).
5.  **Светодиод синий** (для сигнала нормальной температуры).
6.  **2 резистора на 220 Ом**.
7.  **Провода «папа-папа»**.
8.  **(Опционально) Зуммер** для звуковой сигнализации.

### **🔌 Шаг 1: Собираем схему "Термометр с индикацией"**
Собираем схему: датчик температуры как аналоговый вход, два светодиода как выходы для индикации состояния.

![TMP36 Circuit](https://cdn-learn.adafruit.com/assets/assets/000/001/545/medium800/sensors_temp_circuit.jpg)

**Алгоритм сборки:**
1.  **Подключи датчик TMP36:**
    *   **Левый вывод (VCC)** → `5V` на макетной плате.
    *   **Средний вывод (Vout)** → **Аналоговый вход A0** на Arduino.
    *   **Правый вывод (GND)** → `GND` на макетной плате.
    
    **Важно:** Располагай датчик плоской стороной к себе. Если перепутать питание и землю — датчик может перегреться и выйти из строя!

2.  **Подключи светодиоды-индикаторы:**
    *   **Красный светодиод:** Длинная ножка → пин 9 (через резистор 220 Ом), короткая → `GND`.
    *   **Синий светодиод:** Длинная ножка → пин 10 (через резистор 220 Ом), короткая → `GND`.

3.  **Не забудь соединить макетные рельсы `5V` и `GND` с соответствующими пинами Arduino!**

**Проверь себя по чек-листу:**
- [ ] Датчик TMP36 подключён правильно: плоская сторона к тебе, слева-направо: 5V, A0, GND.
- [ ] Оба светодиода подключены через резисторы 220 Ом.
- [ ] Красный светодиод на пине 9, синий — на пине 10.
- [ ] Нет случайных замыканий между ножками датчика.

### **💻 Шаг 2: Пишем код "Умный термостат"**
Загрузи этот код. Arduino будет измерять температуру, выводить её в Монитор порта и зажигать красный светодиод, если температура превысит 25°C, или синий — если ниже.

```cpp
/*
   Занятие 4. Умный термостат.
   Измеряет температуру с помощью датчика TMP36 и включает сигнальные светодиоды.
*/

// Константы с именами пинов
const int TEMP_SENSOR_PIN = A0;   // Датчик температуры на A0
const int RED_LED_PIN = 9;        // Красный светодиод (перегрев)
const int BLUE_LED_PIN = 10;      // Синий светодиод (норма)

// Константы для вычислений
const float REFERENCE_VOLTAGE = 5.0;  // Опорное напряжение (5В)
const int ADC_RESOLUTION = 1023;      // Разрешение АЦП (10 бит = 1023)

// Порог температуры в градусах Цельсия
const float TEMP_THRESHOLD = 25.0;

// Переменные
int rawValue = 0;        // "Сырое" значение с АЦП (0-1023)
float voltage = 0.0;     // Напряжение на выходе датчика (Вольты)
float temperature = 0.0; // Вычисленная температура (градусы Цельсия)

void setup() {
  // Настраиваем пины светодиодов как ВЫХОДЫ
  pinMode(RED_LED_PIN, OUTPUT);
  pinMode(BLUE_LED_PIN, OUTPUT);
  
  // Аналоговый вход (A0) настраивать не нужно
  
  // Инициализируем последовательный порт для вывода данных
  Serial.begin(9600);
  Serial.println("Цифровой термометр с термостатом готов!");
  Serial.println("================================");
}

void loop() {
  // ШАГ 1: Считываем "сырое" значение с датчика
  rawValue = analogRead(TEMP_SENSOR_PIN);
  
  // ШАГ 2: Преобразуем в напряжение (Вольты)
  // Формула: напряжение = (сырое значение / разрешение АЦП) * опорное напряжение
  voltage = rawValue * (REFERENCE_VOLTAGE / ADC_RESOLUTION);
  
  // ШАГ 3: Преобразуем напряжение в температуру (°C) для датчика TMP36
  // Формула: температура = (напряжение - 0.5) * 100
  temperature = (voltage - 0.5) * 100.0;
  
  // ШАГ 4: Выводим все данные в Монитор порта
  Serial.print("Сырое значение: ");
  Serial.print(rawValue);
  Serial.print(" | Напряжение: ");
  Serial.print(voltage, 2); // 2 знака после запятой
  Serial.print("В");
  Serial.print(" | Температура: ");
  Serial.print(temperature, 1); // 1 знак после запятой
  Serial.println("°C");
  
  // ШАГ 5: Принимаем решение на основе температуры
  if (temperature > TEMP_THRESHOLD) {
    // Температура ВЫШЕ порога (жарко)
    digitalWrite(RED_LED_PIN, HIGH);   // Включаем красный светодиод
    digitalWrite(BLUE_LED_PIN, LOW);   // Выключаем синий
    Serial.println("ВНИМАНИЕ: Перегрев! Включен красный сигнал.");
  } else {
    // Температура НИЖЕ или РАВНА порогу (нормально)
    digitalWrite(RED_LED_PIN, LOW);    // Выключаем красный светодиод
    digitalWrite(BLUE_LED_PIN, HIGH);  // Включаем синий
    Serial.println("Температура в норме. Включен синий сигнал.");
  }
  
  // Пауза между измерениями (2 секунды)
  delay(2000);
}
```

**🔍 Что делать после загрузки кода:**
1.  Открой **Инструменты -> Монитор порта** (Ctrl+Shift+M).
2.  Убедись, что скорость (baud rate) выставлена **9600**.
3.  **Наблюдай за показаниями температуры** в реальном времени.
4.  **Попробуй нагреть датчик** пальцем или подуть на него — температура должна измениться, и при превышении 25°C загорится красный светодиод.
5.  **Охлади датчик** (например, прикоснись к нему холодным металлом) — при температуре ниже 25°C должен гореть синий светодиод.

**🎉 Отлично! Ты создал свою первую систему автоматического контроля!**

---

## **🧠 Разбираем код по косточкам**

1.  **Тип данных `float`:**
    ```cpp
    float voltage = 0.0;
    float temperature = 0.0;
    ```
    *   Ключевое слово `float` объявляет переменные для хранения **дробных чисел**.
    *   При инициализации лучше явно указывать дробную часть: `0.0`, а не `0`.

2.  **Вычисления с плавающей точкой:**
    ```cpp
    voltage = rawValue * (REFERENCE_VOLTAGE / ADC_RESOLUTION);
    ```
    *   Важно использовать `.0` у констант, чтобы Arduino выполняла вычисления как `float`, а не как `int`.
    *   `5 / 1023` в целых числах даст `0`, а `5.0 / 1023.0` даст правильное дробное число (~0.00489).

3.  **Формула для TMP36:**
    ```cpp
    temperature = (voltage - 0.5) * 100.0;
    ```
    *   `0.5` — напряжение при 0°C (смещение датчика).
    *   `100.0` — коэффициент преобразования (100 градусов на Вольт, или 10 мВ на градус).

4.  **Вывод чисел с заданной точностью:**
    ```cpp
    Serial.print(voltage, 2); // 2 знака после запятой
    Serial.print(temperature, 1); // 1 знак после запятой
    ```
    *   Второй аргумент в `Serial.print()` для `float` указывает количество знаков **после десятичной точки**.

5.  **Пороговое управление с `if-else`:**
    *   Теперь мы используем измеренное значение (`temperature`) для принятия решения.
    *   `if (temperature > TEMP_THRESHOLD)` — классическое условие сравнения.

---

## **🚀 Проекты для самостоятельной работы**

Выбери проект по интересам. Экспериментируй с порогами, добавляй новые компоненты!

### **🔹 Проект 4.1: "Термометр с графиком"**
Создай визуальное представление температуры в виде **столбчатой диаграммы** из светодиодов.
*   **Что нужно:** 6-8 светодиодов, датчик TMP36.
*   **Логика:** Чем выше температура, тем больше светодиодов загорается в цепочке. Например: <15°C — 1 светодиод, 15-20°C — 2, 20-25°C — 3 и т.д. Можно использовать функцию `map()` для преобразования температуры (например, 10-35°C) в количество светодиодов (1-8).
*   **Усложнение:** Сделай так, чтобы цвет светодиодов менялся от синего (холодно) к зелёному (нормально) и красному (горячо) по мере роста температуры.

### **🔹 Проект 4.2: "Система охлаждения/обогрева"**
Создай модель системы климат-контрля с двумя реле (или просто светодиодами, имитирующими реле).
*   **Что нужно:** Датчик TMP36, 2 светодиода (синий — "охлаждение", красный — "обогрев"), можно добавить зуммер.
*   **Логика:** Установи два порога: нижний (например, 18°C) и верхний (26°C).
    *   Если `temperature < 18°C` — включается "обогрев" (красный светодиод).
    *   Если `temperature > 26°C` — включается "охлаждение" (синий светодиод).
    *   Если между 18 и 26°C — оба выключены (комфортная температура).
*   **Супер-задание:** Добавь гистерезис — чтобы система не включалась/выключалась слишком часто при колебаниях температуры у границы порога.

### **🔹 Проект 4.3: "Цифровой термометр с LCD-дисплеем"**
Выведи показания температуры на **LCD-дисплей 16x2**, чтобы не зависеть от компьютера.
*   **Что нужно:** Датчик TMP36, LCD-дисплей 16x2 (лучше с I2C-интерфейсом — требует всего 2 провода для данных).
*   **Логика:** Подключи дисплей, установи библиотеку `LiquidCrystal_I2C`. Выводи на дисплей текущую температуру с единицами измерения ("Temp: 23.5°C") и статус ("Normal" / "Hot!").
*   **Усложнение:** Добавь отображение максимальной и минимальной температуры за период работы.

---

## **❓ Вопросы на засыпку**

1.  Объясни своими словами, почему в формуле для TMP36 мы вычитаем 0.5 Вольта? Что произойдёт, если забыть это вычитание?
2.  Чем отличается работа с `float` от работы с `int` в Arduino? В каких случаях лучше использовать целые числа?
3.  Представь, что датчик TMP36 вышел из строя и ты должен заменить его на другой, например, LM35, который имеет другую формулу: `temperature = voltage * 100`. Как бы ты изменил код? Почему важно знать характеристики конкретного датчика?
4.  Что такое **гистерезис** в системах автоматики? Зачем он нужен в термостате?
5.  Придумай **три практических применения** для созданного сегодня термостата в домашних условиях (кроме контроля температуры в комнате).

---

## **📌 Критерии успеха: как понять, что ты молодец?**

*   ✅ Схема собрана **без ошибок**, датчик TMP36 подключён с правильной ориентацией.
*   ✅ В **Мониторе порта** отображаются все три значения: сырое, напряжение и температура.
*   ✅ Показания температуры **меняются адекватно** при нагреве/охлаждении датчика.
*   ✅ Светодиоды **корректно переключаются** при пересечении порога 25°C.
*   ✅ Ты **самостоятельно** реализовал как минимум **один** проект и можешь объяснить, как в нём используется измеренная температура.

---

## **🆘 Скорая помощь: если что-то пошло не так...**

*   **Температура показывает нереальные значения (очень высокие или отрицательные):**
    *   **Проверь подключение датчика!** Чаще всего проблема в перепутанных выводах. Убедись, что плоская сторона смотрит на тебя.
    *   Проверь, не перегрелся ли датчик от неправильного подключения.
    *   Убедись, что в коде используются правильные формулы для ТВОЕГО датчика.
*   **Показания температуры не меняются или меняются незначительно:**
    *   Датчик может быть плохо контактировать с окружающей средой. Попробуй подуть на него или аккуратно прикоснись пальцем.
    *   Проверь, не забыл ли ты снять защитное покрытие с датчика (если есть).
*   **Светодиоды не переключаются при изменении температуры:**
    *   Проверь пороговое значение `TEMP_THRESHOLD`. Возможно, температура в комнате всегда выше или всегда ниже этого значения. Попробуй изменить порог на 20°C или 30°C.
    *   Проверь условие в `if`. Убедись, что используешь правильный оператор сравнения (`>`).
*   **В Мониторе порта вместо чисел "NaN" (Not a Number) или бесконечность:**
    *   Это ошибка вычислений с `float`. Проверь, нет ли деления на ноль или математических операций с неинициализированными переменными.
    *   Убедись, что `rawValue` действительно считывается (не равен 0 или 1023 постоянно).

**Главный совет:** Работа с датчиками — это всегда диалог с физическим миром. Всегда проверяй **документацию на конкретный датчик**, особенно формулы преобразования. И помни: даже самые точные датчики нуждаются в **калибровке** — сравнении с эталонным прибором для внесения поправок.
