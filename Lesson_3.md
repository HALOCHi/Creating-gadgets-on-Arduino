# **Занятие 3: Аналоговые пины**

---

## **📖 Теоретическая часть: Аналог vs Цифра.**

### 
До этого мы работали с **цифровыми** сигналами: либо есть напряжение (5В, `HIGH`), либо нет (0В, `LOW`). Но температура, громкость звука, яркость света меняются **плавно**, а не скачками.

*   **Пример из жизни:** Поворот ручки громкости в колонке — это **аналоговое** управление. Кнопка "вкл/выкл" — **цифровое**.

### **2. Как Arduino "понимает" плавность: Аналого-цифровой преобразователь (АЦП)**
У Arduino нет глаз, чтобы видеть градации. Но у неё есть **встроенный АЦП** (ADC — Analog-to-Digital Converter) — специальный "переводчик".
*   **Что делает АЦП:** Измеряет напряжение на аналоговом входе (от 0 до 5 Вольт) и **переводит его в число** от 0 до 1023.
*   **Разрешение 10 бит:** 2^10 = 1024 возможных значения (0-1023). Чем больше значение — тем выше напряжение.

![ADC Principle](https://alexgyver.ru/wp-content/uploads/2020/05/adc-arduino-1.jpg)

### **3. Потенциометр — аналоговое управление**
Потенциометр (переменный резистор, "крутилка") — это главный инструмент для создания аналогового сигнала в наших экспериментах.
*   **Устройство:** Полоска резистивного материала и подвижный контакт (движок). Поворачивая ручку, мы меняем сопротивление между движком и крайними выводами.
*   **Как подключаем:**
    *   Левый вывод → `GND` (0В)
    *   Правый вывод → `5V` (5В)
    *   Средний вывод (движок) → **Аналоговый вход (A0-A5)**
*   **Что получаем:** Поворачивая ручку, на среднем выводе плавно меняется напряжение от 0 до 5В. Arduino считывает это как число от 0 до 1023.

### **4. Как плавно управлять светом: ШИМ (PWM) — хитрость, которая спасает**
Хочешь плавно менять яркость светодиода? Но цифровой пин умеет только `HIGH` (5В) и `LOW` (0В). Решение — **Широтно-Импульсная Модуляция (PWM)**.

**Принцип обмана зрения:** Если очень быстро включать и выключать свет (например, 500 раз в секунду), глаз не увидит мерцания, а увидит **среднюю яркость**. Меняя соотношение времени "включено" к "выключено" (скважность), мы меняем воспринимаемую яркость.

![PWM Principle](https://www.arduino.cc/en/uploads/Tutorial/pwm.gif)

*   **PWM-пины на Arduino Uno:** Отмечены значком **`~`** (пины 3, 5, 6, 9, 10, 11).
*   **Управление:** Функция `analogWrite(pin, value)`, где `value` — число от 0 (всегда выкл) до 255 (всегда вкл). 128 = 50% времени вкл, 50% выкл.

### **5. Функция map() — преобразователь диапазонов**
Часто нужно преобразовать одно значение в другое. Например, поворот потенциометра (0-1023) в яркость светодиода (0-255) или угол сервопривода (0-180).

**`map(value, fromLow, fromHigh, toLow, toHigh)`**
*   Берёт `value` из диапазона `[fromLow...fromHigh]`
*   **Пропорционально переносит** его в новый диапазон `[toLow...toHigh]`
*   Пример: `brightness = map(potValue, 0, 1023, 0, 255);`

🔗 **Полезные ресурсы:**
*   **Гайд по аналоговым входам:** https://alexgyver.ru/lessons/analog-read/
*   **Подробно о PWM:** https://alexgyver.ru/lessons/pwm-signal/

---

## **🛠 Практическая часть: Создаём светодиод с плавным управлением**

**📦 Что нужно из коробки:**
1.  Плата **Arduino Uno**.
2.  **Макетная плата**.
3.  **Потенциометр 10 кОм** (код "103" на корпусе).
4.  **Светодиод (LED)**.
5.  **Резистор на 220 Ом**.
6.  **6 проводов «папа-папа»**.

### **🔌 Шаг 1: Собираем схему "Диммер с потенциометром"**
Собираем двойную схему: потенциометр как аналоговый **вход** и светодиод с ШИМ как **выход**.

![Potentiometer Circuit](https://www.arduino.cc/wiki/static/d3bb5e6b6e4b2f8e8e6d9f7b5e4c2a7b/29114/circuit.png)

**Алгоритм сборки:**
1.  **Подключи потенциометр:**
    *   Левый вывод → `GND` на макетной плате (синяя рельса).
    *   Правый вывод → `5V` на макетной плате (красная рельса).
    *   Средний вывод → **Аналоговый вход A0** на Arduino (проводом).
2.  **Не забудь соединить макетную "землю" и "5V" с соответствующими пинами на Arduino!**
3.  **Подключи светодиод к ШИМ-пину:**
    *   Длинная ножка (+) → **цифровой пин 9** (он с `~`) через резистор 220 Ом.
    *   Короткая ножка (-) → `GND`.

**Проверь себя по чек-листу:**
- [ ] Потенциометр подключён тремя проводами: `GND`, `5V`, `A0`.
- [ ] Светодиод подключён через резистор к пину **9** (а не к 13!).
- [ ] На макетной плате есть общие рельсы `5V` и `GND`, соединённые с Arduino.
- [ ] Потенциометр стоит устойчиво, ручка свободно вращается.

### **💻 Шаг 2: Пишем код "Плавный регулятор яркости"**
Загрузи этот код. Поворачивая ручку потенциометра, ты будешь плавно менять яркость светодиода от полного выключения до максимальной яркости.

```cpp
/*
   Занятие 3. Световой орган с плавным управлением.
   Яркость светодиода плавно регулируется потенциометром.
*/

// Константы с именами пинов
const int POT_PIN = A0;   // Потенциометр подключён к аналоговому входу A0
const int LED_PIN = 9;    // Светодиод подключён к ШИМ-пину 9 (~)

// Переменные
int potValue = 0;         // Сюда будем считывать "сырое" значение с потенциометра (0-1023)
int brightness = 0;       // Сюда будем записывать яркость для светодиода (0-255)

void setup() {
  // Настраиваем пин светодиода как ВЫХОД
  pinMode(LED_PIN, OUTPUT);
  
  // Пины аналоговых входов (A0-A5) НЕ нужно настраивать в pinMode() как INPUT!
  // Они по умолчанию являются входами.
  
  // Начинаем серийное соединение для отладки
  Serial.begin(9600);
  Serial.println("Система готова. Поворачивайте потенциометр.");
}

void loop() {
  // ШАГ 1: Считываем аналоговое значение с потенциометра
  potValue = analogRead(POT_PIN);
  
  // ШАГ 2: Преобразуем диапазон 0-1023 в диапазон 0-255 для ШИМ
  brightness = map(potValue, 0, 1023, 0, 255);
  
  // ШАГ 3: Применяем вычисленную яркость к светодиоду
  analogWrite(LED_PIN, brightness);
  
  // ШАГ 4: Выводим значения в Монитор порта для наглядности
  Serial.print("Потенциометр: ");
  Serial.print(potValue);
  Serial.print(" -> Яркость: ");
  Serial.println(brightness);
  
  // Небольшая задержка для стабильности чтения и чтения в мониторе
  delay(100);
}
```

**🔍 Что делать после загрузки кода:**
1.  Открой **Инструменты -> Монитор порта** (Ctrl+Shift+M).
2.  Убедись, что скорость (baud rate) выставлена **9600**.
3.  **Медленно поворачивай ручку потенциометра** и наблюдай:
    *   Как плавно меняется яркость светодиода.
    *   Как в мониторе порта меняются числа: `potValue` от 0 до 1023 и `brightness` от 0 до 255.

**🎉 Поздравляю! Ты только что создал свой первый полноценный аналоговый интерфейс "человек-машина"!**

---

## **🧠 Разбираем код по косточкам**

1.  **`const int POT_PIN = A0;`**
    *   Обрати внимание: для аналоговых пинов используются **символические имена A0, A1 и т.д.**, которые уже определены в Arduino. Это просто числа (A0 = 14, A1 = 15...), но использовать имена удобнее.

2.  **`pinMode()` для аналоговых входов**
    *   **Важное правило:** Пины A0-A5 при использовании как **аналоговые входы** **НЕ ТРЕБУЮТ** настройки `pinMode()`. Они по умолчанию настроены как входы.
    *   Если ты захочешь использовать их позже как **цифровые** пины, тогда `pinMode()` понадобится.

3.  **`analogRead(POT_PIN)`**
    *   Ключевая функция для работы с аналоговым миром.
    *   **Возвращает целое число от 0 до 1023**, пропорциональное напряжению на указанном аналоговом входе.
    *   Точность измерения: 5В / 1024 = ~4.9 мВ на единицу.

4.  **`map(potValue, 0, 1023, 0, 255)`**
    *   Магическая функция пропорционального пересчёта.
    *   Формула: `map(x, in_min, in_max, out_min, out_max) = (x - in_min) * (out_max - out_min) / (in_max - in_min) + out_min`
    *   В нашем случае: `(potValue - 0) * (255 - 0) / (1023 - 0) + 0 = potValue * 255 / 1023`

5.  **`analogWrite(LED_PIN, brightness)`**
    *   Функция для управления **ШИМ-выходами**.
    *   Работает **только на пинах, помеченных `~`** (3, 5, 6, 9, 10, 11 на Uno).
    *   Второй аргумент — **скважность** (duty cycle) от 0 (0%, всегда LOW) до 255 (100%, всегда HIGH).

---

## **🚀 Проекты для самостоятельной работы**

Выбери проект, который кажется самым интересным. Экспериментируй с диапазонами `map()` и создавай свои световые алгоритмы!

### **🔹 Проект 3.1: "Светофор с ручным управлением длительностью"**
Создай светофор (красный, жёлтый, зелёный светодиоды), где потенциометр будет регулировать **длительность** каждого сигнала.
*   **Что нужно:** 3 светодиода, резисторы, потенциометр.
*   **Логика:** Зелёный горит N секунд → жёлтый 1 сек → красный N секунд → цикл повторяется. Значение N считывается с потенциометра и отображается в Мониторе порта. Диапазон N: от 2 до 10 секунд (используй `map(potValue, 0, 1023, 2, 10)`).
*   **Усложнение:** Добавь второй потенциометр для регулировки длительности жёлтого сигнала.

### **🔹 Проект 3.2: "RGB-смеситель" (основы цветопередачи)**
Используй **RGB-светодиод** (общий катод) или **три обычных светодиода** (красный, зелёный, синий), расположенных близко друг к другу. С помощью **трёх потенциометров** независимо регулируй яркость каждого цвета, создавая любые оттенки.
*   **Что нужно:** RGB-светодиод (или 3 светодиода разных цветов), 3 потенциометра, резисторы (3x220 Ом для отдельных светодиодов или 1x для RGB).
*   **Логика:** Каждый потенциометр подключён к своему аналоговому входу (A0, A1, A2) и управляет своим ШИМ-пином (например, 9, 10, 11). Значения с потенциометров преобразуются в диапазон 0-255 и подаются на соответствующие цветовые каналы.
*   **Супер-задание:** Сделай "палитру" — заранее заданные сочетания цветов, которые можно переключать кнопкой.

### **🔹 Проект 3.3: "Аналоговый измеритель уровня"**
Создай визуальный индикатор уровня, как на аудио-пульте. Чем больше значение с потенциометра, тем больше светодиодов загорается в цепочке.
*   **Что нужно:** 6-10 светодиодов, потенциометр.
*   **Логика:** Считай значение с потенциометра. В зависимости от диапазона, зажигай определённое количество светодиодов. Например, если potValue < 100 — горит 1 светодиод, если 100-200 — 2 светодиода и т.д. Можно использовать несколько `if` или удобную функцию `constrain()` и `map()` для количества светодиодов.
*   **Усложнение:** Сделай так, чтобы светодиоды не просто включались/выключались, а плавно меняли яркость по принципу "градиента" (первый горит ярко, последний — тускло).

---

## **❓ Вопросы на засыпку**

1.  Объясни своими словами, почему аналоговый вход Arduino имеет разрешение 10 бит (0-1023), а ШИМ-выход — только 8 бит (0-255)? Что это означает на практике?
2.  Что произойдёт, если подключить потенциометр неправильно: поменять местами выводы `5V` и `GND`? А если средний вывод подключить не к A0, а к `5V`?
3.  Представь, что нужно сделать, чтобы светодиод загорался только когда потенциометр повёрнут **больше чем наполовину**. Как бы ты изменил код? Напиши условие `if`.
4.  Чем принципиально отличается функция `delay()` от плавного изменения с помощью `analogWrite()` и `map()`? Почему для создания плавных анимаций лучше избегать `delay()`?
5.  Придумай **три неочевидных применения** для связки "потенциометр + Arduino" в быту (кроме регулятора громкости и яркости).

---

## **📌 Критерии успеха: как понять, что ты молодец?**

*   ✅ Схема собрана **без ошибок полярности**, светодиод подключён именно к **ШИМ-пину (~)**.
*   ✅ Яркость светодиода **плавно и без скачков** меняется при повороте потенциометра.
*   ✅ В **Мониторе порта** корректно отображаются значения: `potValue` 0-1023 и `brightness` 0-255.
*   ✅ Ты понимаешь, как работает функция `map()` и можешь объяснить её назначение.
*   ✅ Ты **самостоятельно** реализовал как минимум **один** проект и можешь объяснить логику преобразования значений в нём.

---

## **🆘 Скорая помощь: если что-то пошло не так...**

*   **Светодиод не меняет яркость, а только включается/выключается:**
    *   **Ты подключил светодиод к НЕ ШИМ-пину!** Проверь: на Uno только пины **3, 5, 6, 9, 10, 11** имеют значок `~` и поддерживают `analogWrite()`.
    *   Проверь, правильно ли указан пин в `analogWrite()`.
*   **Яркость меняется, но скачками, а не плавно:**
    *   Возможно, у тебя **плохой (старый) потенциометр** с "подгоревшим" или изношенным резистивным слоем. Попробуй другой.
    *   Проверь соединения — плохой контакт может давать скачки.
*   **В мониторе порта potValue всегда 0 или всегда 1023:**
    *   **Всегда 0:** Средний вывод потенциометра, скорее всего, закорочен на `GND` или оторван от `A0`. Проверь провод.
    *   **Всегда 1023:** Средний вывод закорочен на `5V` или оторван. Также проверь, подано ли питание (`5V` и `GND`) на крайние выводы потенциометра.
*   **Светодиод горит с постоянной яркостью, не реагируя на потенциометр:**
    *   Проверь, не перепутал ли ты аналоговый вход. Ты подключил средний вывод к `A0`, а в коде читаешь, например, `A1`?
    *   Убедись, что в коде есть `analogWrite()`, а не `digitalWrite()`.

**Главный совет:** Аналоговый мир более "чувствительный", чем цифровой. Здесь важно качество соединений и компонентов. Если что-то не работает, в первую очередь проверь **целостность цепи потенциометра (5V→A0→GND)** с помощью мультиметра или просто пересобери схему заново.
