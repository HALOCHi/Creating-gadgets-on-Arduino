# **Занятие 6: Ультразвуковой дальномер HC-SR04 — "эхолокация" на Arduino**

---

## **🎯 Цель занятия:**
Превратить Arduino в сонар, как у летучей мыши или подводной лодки! Сегодня ты научишься измерять расстояние до объектов без прикосновения, используя ультразвук. Ты освоишь принцип эхолокации, научишься работать с временными интервалами и создашь несколько полезных устройств: от простого дальномера до "умной" сигнализации.

---

## **📖 Теоретическая часть: Как "видеть" ушами — принцип эхолокации**

### **1. Что такое ультразвук и почему его используют?**
Звук — это механические волны, распространяющиеся в среде (воздухе, воде). Человек слышит звуки частотой от **20 Гц до 20 кГц** (20 000 колебаний в секунду).

**Ультразвук** — это звуковые волны с частотой **ВЫШЕ 20 кГц**, которые человеческое ухо не слышит.

**Почему используют ультразвук для измерений?**
1.  **Направленность:** Ультразвуковые волны хорошо фокусируются в узкий луч.
2.  **Неслышимость:** Не создают шумового загрязнения.
3.  **Надёжность:** Мало подвержены помехам от обычных звуков.

**Где применяется:**
- Медицина (УЗИ)
- Эхолоты кораблей
- Датчики парковки в автомобилях
- Наши дальномеры HC-SR04!

### **2. Принцип работы HC-SR04: "Крикни и послушай эхо"**
![Принцип работы HC-SR04](https://cdn.sparkfun.com/assets/learn_tutorials/3/1/9/diagram.png)

Датчик работает по принципу **эхолокации**:
1.  **Излучатель (Transmitter)** отправляет короткий ультразвуковой импульс (40 кГц).
2.  Импульс летит до объекта, отражается от него.
3.  **Приёмник (Receiver)** улавливает отражённый сигнал (эхо).
4.  Arduino измеряет **время между отправкой и приёмом**.

### **3. Магия формулы: от времени к расстоянию**
**Известно:**
- Скорость звука в воздухе при 20°C ≈ **340 м/с** или **0,034 см/мкс** (сантиметров за микросекунду)
- Микросекунда (мкс) = 0,000001 секунды

**Формула:**
```
Расстояние = (Время_полёта × Скорость_звука) ÷ 2
```

**Почему делим на 2?** Потому что сигнал проходит путь **туда и обратно**, а нам нужно расстояние только в одну сторону — от датчика до объекта.

**В коде это выглядит так:**
```cpp
// duration — время в микросекундах
distance_cm = duration * 0.034 / 2;
```

### **4. Анатомия датчика HC-SR04 (4 контакта)**
![Распиновка HC-SR04](https://lastminuteengineers.b-cdn.net/wp-content/uploads/arduino/HC-SR04-Pinout.png)

- **VCC** → **5V** (питание)
- **Trig** (Trigger) → **цифровой выход** с Arduino. Подаём импульс для запуска измерения.
- **Echo** → **цифровой вход** на Arduino. Считываем отражённый сигнал.
- **GND** → **GND** (земля)

### **5. Алгоритм работы (последовательность команд)**
1.  Подаём на **Trig** высокий сигнал на **10 микросекунд**.
2.  Датчик автоматически отправляет 8 импульсов ультразвука (40 кГц).
3.  На пине **Echo** устанавливается высокий уровень (`HIGH`).
4.  Ждём, пока отражённый сигнал не вернётся — тогда Echo станет `LOW`.
5.  Измеряем длительность высокого сигнала на Echo функцией `pulseIn()`.
6.  Рассчитываем расстояние по формуле.

🔗 **Полезные ресурсы:**
*   **Документация на HC-SR04:** https://cdn.sparkfun.com/datasheets/Sensors/Proximity/HCSR04.pdf
*   **Подробный гайд по ультразвуковым датчикам:** https://alexgyver.ru/lessons/hc-sr04/

---

## **🛠 Практическая часть: Создаём "Умный дальномер" с световой и звуковой сигнализацией**

**📦 Что нужно из коробки:**
1.  Плата **Arduino Uno**.
2.  **Макетная плата**.
3.  **Ультразвуковой датчик HC-SR04**.
4.  **Светодиод красный**.
5.  **Светодиод зелёный**.
6.  **Пьезоизлучатель (зуммер)**.
7.  **2 резистора на 220 Ом** (для светодиодов).
8.  **Провода «папа-папа»**.
9.  **Провода «папа-мама»** (4 шт., для удобного подключения HC-SR04).

### **🔌 Шаг 1: Собираем схему "Дальномер с сигнализацией"**
Собираем систему: HC-SR04 как измеритель, светодиоды и зуммер как система оповещения.

![Схема HC-SR04 с сигнализацией](https://lastminuteengineers.b-cdn.net/wp-content/uploads/arduino/Ultrasonic-Distance-Sensor-Arduino-Circuit-Diagram.png)

**Алгоритм сборки:**
1.  **Подключи HC-SR04:**
    - **VCC** → `5V` на Arduino
    - **Trig** → **цифровой пин 9** на Arduino
    - **Echo** → **цифровой пин 10** на Arduino
    - **GND** → `GND` на Arduino

    **Важно:** Некоторые датчики HC-SR04 имеют 5 контактов (есть дополнительный контакт для режима). У классического — 4 контакта.

2.  **Подключи светодиоды и зуммер:**
    - **Красный светодиод:** Длинная ножка → пин 11 (через резистор 220 Ом), короткая → `GND`.
    - **Зелёный светодиод:** Длинная ножка → пин 12 (через резистор 220 Ом), короткая → `GND`.
    - **Зуммер:** Красный провод (+) → пин 8, чёрный провод (-) → `GND`.

3.  **Проверь:** Все `GND` соединены вместе (датчик, светодиоды, зуммер, Arduino).

**Проверь себя по чек-листу:**
- [ ] HC-SR04 подключён правильно: VCC→5V, Trig→9, Echo→10, GND→GND
- [ ] Светодиоды подключены через резисторы
- [ ] Зуммер подключён: плюс к пину 8, минус к GND
- [ ] Нет случайных коротких замыканий

### **💻 Шаг 2: Пишем код "Умная парковочная система"**
Загрузи этот код. Система будет измерять расстояние и:
- При расстоянии > 50 см: горит зелёный светодиод (можно ехать)
- При расстоянии 20-50 см: горит жёлтый (оба светодиода, внимание!)
- При расстоянии < 20 см: горит красный и пищит зуммер (опасность!)

```cpp
/*
   Занятие 6. Умная парковочная система на HC-SR04.
   Измеряет расстояние до препятствия и включает световую/звуковую сигнализацию.
*/

// Константы с именами пинов
const int TRIG_PIN = 9;     // Trig подключен к пину 9
const int ECHO_PIN = 10;    // Echo подключен к пину 10
const int RED_LED_PIN = 11; // Красный светодиод
const int GREEN_LED_PIN = 12; // Зелёный светодиод
const int BUZZER_PIN = 8;   // Пьезоизлучатель

// Пороговые значения расстояния (в сантиметрах)
const int SAFE_DISTANCE = 50;    // Безопасное расстояние (зелёная зона)
const int WARNING_DISTANCE = 20; // Опасное расстояние (красная зона)

// Переменные
long duration = 0;      // Длительность импульса на Echo (микросекунды)
int distance = 0;       // Расстояние до объекта (сантиметры)

void setup() {
  // Инициализируем последовательный порт для отладки
  Serial.begin(9600);
  Serial.println("Ультразвуковой дальномер HC-SR04 готов к работе!");
  
  // Настраиваем пины
  pinMode(TRIG_PIN, OUTPUT);    // Trig - выход (мы им управляем)
  pinMode(ECHO_PIN, INPUT);     // Echo - вход (мы его читаем)
  pinMode(RED_LED_PIN, OUTPUT);
  pinMode(GREEN_LED_PIN, OUTPUT);
  pinMode(BUZZER_PIN, OUTPUT);
  
  // Гасим светодиоды и выключаем зуммер при старте
  digitalWrite(RED_LED_PIN, LOW);
  digitalWrite(GREEN_LED_PIN, LOW);
  noTone(BUZZER_PIN);
  
  delay(100);
}

void loop() {
  // ШАГ 1: Очищаем состояние пина Trig
  digitalWrite(TRIG_PIN, LOW);
  delayMicroseconds(2); // Ждём 2 микросекунды
  
  // ШАГ 2: Подаём импульс на Trig длиной 10 микросекунд
  digitalWrite(TRIG_PIN, HIGH);
  delayMicroseconds(10);
  digitalWrite(TRIG_PIN, LOW);
  
  // ШАГ 3: Измеряем длительность высокого сигнала на пине Echo
  // pulseIn() ждёт, пока пин перейдёт в нужное состояние, и измеряет время в микросекундах
  duration = pulseIn(ECHO_PIN, HIGH);
  
  // ШАГ 4: Рассчитываем расстояние
  // Скорость звука 0.034 см/мкс, делим на 2 (туда и обратно)
  distance = duration * 0.034 / 2;
  
  // ШАГ 5: Выводим результат в монитор порта
  Serial.print("Расстояние: ");
  Serial.print(distance);
  Serial.println(" см");
  
  // ШАГ 6: Сигнализация в зависимости от расстояния
  if (distance > SAFE_DISTANCE) {
    // Зелёная зона: безопасно (> 50 см)
    digitalWrite(GREEN_LED_PIN, HIGH);
    digitalWrite(RED_LED_PIN, LOW);
    noTone(BUZZER_PIN);
    Serial.println("Статус: БЕЗОПАСНО");
  }
  else if (distance > WARNING_DISTANCE && distance <= SAFE_DISTANCE) {
    // Жёлтая зона: внимание (20-50 см) - мигаем жёлтым (оба светодиода)
    digitalWrite(GREEN_LED_PIN, HIGH);
    digitalWrite(RED_LED_PIN, HIGH);
    noTone(BUZZER_PIN);
    Serial.println("Статус: ВНИМАНИЕ");
  }
  else {
    // Красная зона: опасно (< 20 см) - красный свет и звуковая сигнализация
    digitalWrite(GREEN_LED_PIN, LOW);
    digitalWrite(RED_LED_PIN, HIGH);
    
    // Зуммер пищит с частотой, зависящей от расстояния
    // Чем ближе объект, тем выше частота писка
    int frequency = map(distance, 0, WARNING_DISTANCE, 2000, 500);
    tone(BUZZER_PIN, frequency, 200); // Пищим 200 мс
    
    Serial.println("Статус: ОПАСНОСТЬ!");
  }
  
  // Пауза между измерениями (100 мс)
  delay(100);
}
```

**🔍 Что делать после загрузки кода:**
1.  Открой **Инструменты -> Монитор порта** (Ctrl+Shift+M), скорость **9600**.
2.  **Поднеси руку к датчику** и наблюдай:
    - Как меняются показания расстояния в мониторе порта
    - Как переключаются светодиоды при переходе через пороги 50 см и 20 см
    - Как зуммер начинает пищать при приближении руки ближе 20 см, причём частота меняется
3.  **Поэкспериментируй** с разными объектами: книга, ткань, металлическая поверхность. Какие лучше отражают ультразвук?

**🎉 Потрясающе! Ты создал систему, которая "видит" пространство и реагирует на него!**

---

## **🧠 Разбираем код по косточкам**

1.  **`delayMicroseconds()` vs `delay()`:**
    - `delayMicroseconds(2)` — задержка в **микросекундах** (1 мкс = 0,001 мс). Используется для точного управления импульсами.
    - `delay(100)` — задержка в **миллисекундах**.

2.  **`pulseIn(ECHO_PIN, HIGH)`:**
    - Ключевая функция для работы с HC-SR04.
    - **Ждёт**, пока указанный пин перейдёт в состояние `HIGH`, затем измеряет, **сколько времени** он остаётся в этом состоянии.
    - Возвращает время в **микросекундах**.
    - Имеет таймаут (по умолчанию 1 секунда), после которого возвращает 0.

3.  **Расчёт расстояния:**
    ```cpp
    distance = duration * 0.034 / 2;
    ```
    - `duration` — время в микросекундах
    - `0.034` — скорость звука в см/мкс (340 м/с = 0,034 см/мкс)
    - `/ 2` — потому что сигнал прошёл путь туда и обратно

4.  **Каскад условий `if-else if-else`:**
    - Позволяет проверить несколько условий по очереди.
    - Как только одно условие выполняется, остальные пропускаются.
    - Идеально для зонирования (безопасно/внимание/опасно).

5.  **Динамическая частота зуммера:**
    ```cpp
    int frequency = map(distance, 0, WARNING_DISTANCE, 2000, 500);
    tone(BUZZER_PIN, frequency, 200);
    ```
    - `map()` преобразует расстояние (0-20 см) в частоту (2000-500 Гц).
    - Чем меньше расстояние → тем выше частота → тем "тревожнее" звук.
    - `tone()` с третьим параметром звучит заданное время (200 мс), затем автоматически выключается.

---

## **🚀 Проекты для самостоятельной работы**

### **🔹 Проект 6.1: "Цифровая рулетка" с LCD-дисплеем**
Создай электронный измерительный инструмент, который показывает расстояние на дисплее.
*   **Что нужно:** HC-SR04, **LCD-дисплей 16x2** (лучше с I2C), кнопка.
*   **Логика:** При нажатии кнопки фиксируется текущее расстояние и выводится на дисплей. Можно добавить функцию удержания минимального/максимального значения.
*   **Усложнение:** Реализуй режим непрерывных измерений с графиком изменения расстояния в мониторе порта.

### **🔹 Проект 6.2: "Умный светильник с датчиком присутствия"**
Создай светильник, который включается автоматически, когда кто-то приближается.
*   **Что нужно:** HC-SR04, **яркий светодиод** или несколько светодиодов, резисторы.
*   **Логика:** При обнаружении объекта на расстоянии менее 30 см свет плавно загорается (используй ШИМ и `analogWrite()`). При удалении объекта — плавно гаснет. Добавь гистерезис, чтобы свет не мигал при колебаниях на границе.
*   **Супер-задание:** Сделай так, чтобы яркость зависела от расстояния: чем ближе человек, тем ярче свет.

### **🔹 Проект 6.3: "Система подсчёта объектов"**
Создай устройство, которое считает, сколько предметов прошло через контролируемую зону.
*   **Что нужно:** HC-SR04, два светодиода (вход/выход), LCD-дисплей или вывод в монитор порта.
*   **Логика:** Расположи датчик так, чтобы объекты проходили на фиксированном расстоянии. При резком уменьшении расстояния (предмет вошёл в зону) и последующем восстановлении (предмет вышел) увеличивай счётчик.
*   **Алгоритм:** Нужно отслеживать не просто расстояние, а **резкие изменения**. Можно хранить предыдущее значение и сравнивать с текущим.

---

## **❓ Вопросы на засыпку**

1.  Объясни своими словами, почему датчик HC-SR04 не может точно измерить расстояние до пушистого ковра или мягкой подушки? С какими ещё материалами будут проблемы?
2.  Что произойдёт, если перед датчиком окажется объект под углом (не перпендикулярно лучу)? Как это повлияет на точность?
3.  Почему в коде мы используем `pulseIn(ECHO_PIN, HIGH)`, а не `pulseIn(ECHO_PIN, LOW)`? Что измеряет каждый из этих вариантов?
4.  Представь, что тебе нужно измерить расстояние до объекта, который находится в 3 метрах. Сработает ли HC-SR04? Почему? (Максимальная дальность HC-SR04 — около 4 метров).
5.  Придумай **три нестандартных применения** ультразвукового дальномера в быту, кроме парковочного радара и автоматического выключателя света.

---

## **📌 Критерии успеха: как понять, что ты молодец?**

*   ✅ Дальномер **стабильно измеряет расстояние** с точностью до 1-2 см.
*   ✅ Светодиоды **чётко переключаются** при пересечении пороговых значений.
*   ✅ Зуммер **изменяет частоту** в зависимости от расстояния.
*   ✅ Понимаешь принцип работы `pulseIn()` и формулу расчёта расстояния.
*   ✅ **Самостоятельно** реализовал один из проектов и можешь объяснить его логику.

---

## **🆘 Скорая помощь: если что-то пошло не так...**

*   **В мониторе порта постоянно 0 см или очень маленькое значение:**
    - Перед датчиком слишком близко находится объект (рука, стол).
    - **Перепутаны Trig и Echo!** Самая частая ошибка. Поменяй провода местами.
    - Неисправный датчик (попробуй другой).

*   **Постоянно показывает около 2-3 см:**
    - Датчик "видит" свою собственную рамку (эффект кромки). Отодвинь объект подальше.
    - Плохой контакт в проводах.

*   **Показывает случайные большие числа (300-400 см):**
    - `pulseIn()` не дождался сигнала и вернул 0. Умножение 0 по формуле даст 0, но если duration случайно большое — получатся огромные числа.
    - **Объект слишком далеко** (более 4 метров) или **слишком плохо отражает** ультвук (ткань, ковёр).
    - Эхо не вернулось — проверь, направлен ли датчик на твёрдую поверхность.

*   **Зуммер не пищит или пищит постоянно:**
    - Проверь подключение зуммера: плюс к пину 8, минус к GND.
    - Убедись, что в "безопасной" зоне вызывается `noTone(BUZZER_PIN)`.
    - Некоторые зуммеры активные (издают звук при подаче напряжения), а некоторые пассивные (требуют генерации тона). У нас пассивный — он управляется только функцией `tone()`.

*   **Датчик сильно греется:**
    - **Срочно отключи!** Ты перепутал VCC и GND. При правильном подключении HC-SR04 почти не греется.

**Профессиональный совет:** Для точных измерений учитывай **температуру воздуха**, так как скорость звука от неё зависит. Формула: `скорость_звука = 331.3 + 0.606 * температура_°C` (в м/с). Для комнатных условий (20°C) 340 м/с — хорошее приближение. Для уличных проектов можешь добавить датчик температуры и корректировать скорость звука в реальном времени!

---
