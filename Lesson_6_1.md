## **Занятие 6: Датчики температуры и влажности DHT11/DHT22**

![3968 750](https://github.com/user-attachments/assets/6a50be58-3c89-4584-9d9b-fcf9b61c2a4d)

**🎯 Цель занятия:**

Научиться измерять температуру и влажность с помощью датчиков DHT11/DHT22, чтобы использовать эти данные в проектах умного дома, метеостанций или контроля условий для растений.

---

## 📖 **Теоретическая часть: Как это работает?**

**1. DHT11 vs DHT22: В чём разница?**
Просто запомни:
*   **DHT11** — дешёвый, но менее точный. Измеряет влажность 20-80% (±5%) и температуру 0-50°C (±2°C). Опрашивать можно раз в секунду.
*   **DHT22** — дороже, но точнее и шире диапазон. Измеряет влажность 0-100% (±2-5%) и температуру -40 до +80°C (±0.5°C). **Опрашивать не чаще, чем раз в 2 секунды!** (Это важно, иначе будут ошибки).

**2. Что внутри датчика?**
Датчик содержит **две штуки**:
*   **Ёмкостной датчик влажности**: измеряет, сколько воды «впитал» воздух. Чем выше влажность, тем выше ёмкость.
*   **Термистор (NTC)**: специальный резистор, сопротивление которого меняется с температурой.

**3. Распиновка (подключение)**
На модуле с платой (самый удобный вариант) обычно 3 пина:

```
    DHT Модуль (вид спереди, пины вниз)
    ┌─────────────┐
    │    [ ]      │ ← Сам датчик
    │             │
    ├──────┬──────┤
    │  VCC │ DATA │  GND
    └──────┴──────┘
```

**🔥 ВАЖНО:** Если у тебя **голый датчик** (4 ноги), обязательно добавь **резистор 10 кОм между VCC и DATA**.

---

## 🔗 **Полезные ресурсы:**

*  **https://3d-diy.ru/blog/datchik-vlazhnosti-i-temperatury-dht11/?srsltid=AfmBOor3gMygtuyu5FNrWMj1A_jBNLXZf0gtuCa0cWdUU0rfcTUua2Io**
*  **https://robotclass.ru/tutorials/arduino-temperature-sensor/**


---

## 🛠 **Практическая часть: Базовая метеостанция**

### **📦 Что нужно:**
*   Arduino Uno
*   Макетная плата
*   **Модуль DHT11 или DHT22** (с готовой платой)
*   Провода

### **🔌 Схема подключения:**
```
DHT Модуль    Arduino
V          -> 5V
S          -> Цифровой пин 2
G          -> GND
```

**Примечание:** Пин DATA можно подключить к любому цифровому пину, просто в коде потом нужно будет указать правильный номер.

### **💻 Код: Вывод в Serial Monitor**

**Шаг 1: Установка библиотеки**
1.  В Arduino IDE: **Скетч → Подключить библиотеку → Управлять библиотеками...**
2.  В поиске введи: **`DHT sensor library`**
3.  Установи библиотеку **от Adafruit** (самая популярная и стабильная).

**Шаг 2: Загружаем код**
```cpp
#include <DHT.h>

// Настройка: УКАЖИ СВОЙ ДАТЧИК И ПИН!
#define DHTPIN 2           // Пин, куда подключен DATA
#define DHTTYPE DHT22      // Тип датчика: DHT11 или DHT22

DHT dht(DHTPIN, DHTTYPE);  // Создаём объект датчика

void setup() {
  Serial.begin(9600);
  Serial.println(F("Тест датчика DHT..."));
  dht.begin();             // Запускаем датчик
}

void loop() {
  // ЖДЁМ между измерениями! Для DHT22 - минимум 2 секунды!
  delay(2000);

  // Читаем данные. Если что-то пошло не так - переменные станут NaN (Not a Number)
  float humidity = dht.readHumidity();      // Влажность в %
  float tempC = dht.readTemperature();      // Температура в °C
  // Для чтения в Фаренгейтах: dht.readTemperature(true);

  // Проверяем, всё ли прочиталось нормально
  if (isnan(humidity) || isnan(tempC)) {
    Serial.println(F("Ошибка чтения!"));
    return; // Выходим и пробуем снова в следующем цикле
  }

  // Выводим в монитор порта
  Serial.print(F("Влажность: "));
  Serial.print(humidity);
  Serial.print(F("%  |  Температура: "));
  Serial.print(tempC);
  Serial.println(F("°C"));

  // Бонус: вычисляем и выводим "ощущаемую" температуру (Heat Index)
  float heatIndex = dht.computeHeatIndex(tempC, humidity, false);
  Serial.print(F("Ощущается как: "));
  Serial.print(heatIndex);
  Serial.println(F("°C"));
  Serial.println(F("---")); // Разделитель
}
```

---

## 🚀 **Проекты для самостоятельной работы**

### **🔹 Проект 1: Термометр со световой индикацией**
*   **Цель:** Визуально показывать, комфортная ли температура.
*   **Что добавить:** 3 светодиода (синий, зёленый, красный) и резисторы 220 Ом.
*   **Логика:**
    *   `< 18°C` — горит **синий** (холодно)
    *   `18-26°C` — горит **зелёный** (норма)
    *   `> 26°C` — горит **красный** (жарко)
*   **Совет:** Используй `if...else if...else` и `digitalWrite()`.

### **🔹 Проект 2: Гигрометр для растений с сигнализацией**
*   **Цель:** Сигнализировать, когда растение нужно полить.
*   **Что добавить:** Зуммер (пьезодинамик) и любой светодиод.
*   **Логика:**
    *   `Влажность < 40%` — включается **зуммер** (пищит) и **мигает светодиод**.
    *   `Влажность > 40%` — тишина, светодиод выключен.
*   **Совет:** Для мигания без `delay()` используй `millis()`. Для звука — `tone()`.

### **🔹 Проект 3: Метеостанция с графиком (проще, чем кажется!)**
*   **Цель:** Видеть, как температура и влажность меняются в реальном времени.
*   **Что добавить:** Ничего, только компьютер.
*   **Логика:** Используем **Serial Plotter** (встроен в Arduino IDE).
*   **Совет:** Замени вывод в коде на это, чтобы Plotter понял:
    ```cpp
    Serial.print(tempC);
    Serial.print(" ");
    Serial.println(humidity);
    ```
    Открой **Инструменты → Serial Plotter** и наблюдай за двумя графиками!

### **🔹 Проект 4: Умный вентилятор/обогреватель (имитация)**
*   **Цель:** Автоматически «включать» устройства для поддержания температуры.
*   **Что добавить:** 2 светодиода (синий для «вентилятора», красный для «обогревателя»).
*   **Логика:**
    *   `Температура > 28°C` — включается **синий** светодиод («вентилятор»).
    *   `Температура < 16°C` — включается **красный** светодиод («обогреватель»).
    *   `Между 16 и 28°C` — оба выключены.

---

## ❓ **Вопросы для проверки себя**
1.  Почему DHT22 нельзя опрашивать чаще, чем раз в 2 секунды?
2.  Что нужно добавить в схему, если используешь датчик **без готовой платы** (голый, с 4 ногами)?
3.  В коде есть проверка `if (isnan(humidity) || isnan(tempC))`. Зачем она?
4.  В чём главное практическое отличие DHT11 от DHT22 для твоего проекта?
5.  Как бы ты защитил уличный датчик от дождя, но оставил доступ воздуха?

---

---

## 📌 **Критерии успеха:**
*   ✅ Дал ответы на вопросы для самопроверки.
*   ✅ Собрал схему, загрузил код и увидел корректные показания в Serial Monitor.
*   ✅ Реализовал хотя бы **один** проект из списка выше.
*   ✅ В коде своих проектов есть комментарии, объясняющие логику.

---

## 🆘 **Главные проблемы и их решение:**
*   **«Ошибка чтения» или «Failed to read from DHT sensor»:**
    *   **Проверь питание (5V)** и соединение **GND**.
    *   **Добавь резистор 10 кОм** между DATA и VCC, если его нет на плате.
    *   Увеличь паузу (`delay`) до 2500 мс.
    *   Попробуй другой цифровой пин.
*   **Показывает нереальные значения (например, влажность 99.9%):**
    *   Скорее всего, **перепутаны пины** VCC и DATA. Проверь.
    *   Возможно, **неправильно указан тип датчика** в строке `#define DHTTYPE`.
*   **Датчик греется после подключения:**
    *   **Немедленно отключи!** Ты перепутал **VCC и GND**. Проверь схему.

---

## 🏆 **Финальное задание:**
Сделай проекты из списка выше или придумай свой. Собери его, напиши код и протестируй. Опиши:
1.  **Что делает твоё устройство?**
2.  **Какую проблему решает?**
3.  **С какими сложностями столкнулся и как решил?**
4.  **Что можно улучшить в будущем?**
