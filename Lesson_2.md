# **Занятие 2: Кнопка — диалог с устройством**

---

## **🎯 Цель занятия:**
Научить Arduino "слушать" и "понимать" команды из реального мира! Сегодня ты создашь первое интерактивное устройство, где физическое действие (нажатие кнопки) будет управлять светом. Ты поймёшь, как цифровой вход работает изнутри, и соберёшь систему, которая реагирует на твои прикосновения.

---

## **📖 Теоретическая часть: Как Arduino "чувствует" нажатие**

### **1. От выхода к входу: новая роль пина**
На прошлом занятии мы использовали пин как **выход (OUTPUT)** — команды шли **от программы к миру**. Сегодня мы научим пину быть **входом (INPUT)** — информация будет поступать **от мира к программе**.

*   **Аналогия с Python:** 
    *   Выход — это `print()`, который выводит текст.
    *   Вход — это `input()`, который считывает текст с клавиатуры.

### **2. Устройство кнопки: просто, как дверной звонок**
Кнопка (или тумблер) — это механический переключатель, который **замыкает** или **размыкает** электрическую цепь.
*   **В состоянии покоя:** Контакты внутри кнопки **разомкнуты**. Ток не течёт.
*   **При нажатии:** Контакты **замыкаются**. Ток начинает течь.

![Button Anatomy](https://www.arduino.cc/wiki/static/2e4cb2c6712146a2b3f3b27b8302c3e5/29114/button.png)

### **3. Проблема "плавающего" пина и её решение**
Представь себе вход пина Arduino как очень чувствительный микрофон. Если к нему ничего не подключено, он будет "ловить" случайные электромагнитные помехи (от WiFi, телефона, даже от твоего движения). Это называется **"плавающий вход"** — его значение будет случайно "дёргаться" между `HIGH` и `LOW`.

**Решение: подтягивающий резистор (Pull-up Resistor)**
Мы искусственно "притягиваем" вход к стабильному состоянию (`HIGH` или `LOW`), когда кнопка не нажата, с помощью резистора.

*   **Внешний подтягивающий резистор:** Мы можем собрать схему сами, используя резистор 10 кОм.
*   **Встроенный подтягивающий резистор (INPUT_PULLUP):** У Arduino внутри уже есть такие резисторы! Их можно включить программно. Это удобнее и требует меньше деталей. **Используем именно этот способ.**

### **4. Логика работы кнопки с INPUT_PULLUP**
Важнейший момент, который нужно запомнить:

**Когда кнопка НЕ нажата (отпущена)**: Вход через внутренний резистор подключён к **+5V (`HIGH`)**.  
**Когда кнопка нажата**: Вход замыкается на **GND (`LOW`)**.

Получается **обратная логика**: `HIGH` = не нажато, `LOW` = нажато.

![INPUT_PULLUP](https://docs.arduino.cc/static/4f5a4b0f3e4d3e3f7d8c9b8e8f9a0b1c/29114/input_pullup.png)

### **5. Алгоритм работы программы с кнопкой**
1.  **Настроить пин кнопки** как `INPUT_PULLUP`.
2.  **Считать состояние пина** в переменную с помощью `digitalRead()`.
3.  **Проанализировать значение**: если `LOW` — кнопка нажата, если `HIGH` — отпущена.
4.  **Выполнить действие** на основе состояния кнопки.

🔗 **Полезные ресурсы:**
*   **Официальный гайд по кнопке:** https://www.arduino.cc/en/Tutorial/BuiltInExamples/Button
*   **Подробнее о INPUT_PULLUP:** https://alexgyver.ru/lessons/pinmode-and-pullup/

---

## **🛠 Практическая часть: Собираем "Умный переключатель"**

**📦 Что нужно из коробки:**
1.  Плата **Arduino Uno**.
2.  **Макетная плата**.
3.  **Тактовая кнопка**.
4.  **Светодиод (LED)**.
5.  **Резистор на 220 Ом** (для светодиода).
6.  **3 провода «папа-папа»**.

### **🔌 Шаг 1: Собираем схему "Кнопка + Светодиод"**
Собираем по схеме. Обрати внимание: у кнопки 4 ножки, но они соединены попарно. Мы используем две диагональные ножки.

![Button Circuit](https://www.arduino.cc/wiki/static/2e4cb2c6712146a2b3f3b27b8302c3e5/29114/circuit.png)

**Алгоритм сборки:**
1.  **Собери схему со светодиодом** как на прошлом занятии (длинная ножка → пин 13 через провод, короткая → GND через резистор 220 Ом).
2.  **Вставь кнопку** так, чтобы она перекрывала центральную канавку макетной платы.
3.  **Подключи одну диагональную ножку кнопки к GND.** Используй провод.
4.  **Подключи противоположную диагональную ножку к цифровому пину 2** на Arduino. **Это наш вход.**

**Важно:** Мы **НЕ** используем отдельный резистор для кнопки, потому что задействуем встроенный подтягивающий (`INPUT_PULLUP`)!

**Проверь себя по чек-листу:**
- [ ] Светодиод подключён правильно (резистор на катоде к GND).
- [ ] Кнопка стоит перпендикулярно канавке, две ножки с одной стороны, две с другой.
- [ ] Одна пара диагональных ножек кнопки: одна ножка → GND, противоположная → пин 2 на Arduino.
- [ ] Нет лишних соединений, которые могут закоротить цепь.

### **💻 Шаг 2: Пишем код "Свети, пока держу"**
Загрузи этот код. Он зажжёт светодиод только тогда, когда ты **удерживаешь кнопку нажатой**.

```cpp
/*
   Занятие 2. Умный переключатель.
   Светодиод горит только при нажатой кнопке.
*/

// Константы с именами пинов для удобства
const int BUTTON_PIN = 2;   // Пин, к которому подключена кнопка
const int LED_PIN = 13;     // Пин, к которому подключен светодиод

void setup() {
  // Настраиваем пин светодиода как ВЫХОД
  pinMode(LED_PIN, OUTPUT);
  
  // Настраиваем пин кнопки как ВХОД со ВСТРОЕННЫМ подтягивающим резистором к HIGH
  pinMode(BUTTON_PIN, INPUT_PULLUP);
  
  // Начинаем серийное соединение для отладки (можно смотреть в Монитор порта)
  Serial.begin(9600);
  Serial.println("Система готова. Нажмите кнопку.");
}

void loop() {
  // Считываем состояние кнопки. Помни: LOW - нажата, HIGH - отпущена.
  int buttonState = digitalRead(BUTTON_PIN);
  
  // Отправляем состояние в Монитор порта для отладки
  Serial.print("Состояние кнопки: ");
  Serial.println(buttonState);
  
  // Проверяем: если кнопка нажата (buttonState == LOW)...
  if (buttonState == LOW) {
    digitalWrite(LED_PIN, HIGH); // ... зажигаем светодиод
    Serial.println("Кнопка нажата. Свет ВКЛ.");
  } else {
    digitalWrite(LED_PIN, LOW);  // ... иначе гасим светодиод
    Serial.println("Кнопка отпущена. Свет ВЫКЛ.");
  }
  
  // Небольшая задержка для стабильности считывания и чтения в мониторе
  delay(100);
}
```

**🔍 Что делать после загрузки кода:**
1.  Открой **Инструменты -> Монитор порта** (Ctrl+Shift+M).
2.  Убедись, что скорость (baud rate) выставлена **9600**.
3.  Нажми на кнопку и наблюдай, как загорается светодиод, а в мониторе появляются сообщения.
4.  Отпусти кнопку — светодиод погаснет.

**🎉 Отлично! Теперь у тебя есть простое, но полностью рабочее устройство управления светом!**

---

## **🧠 Разбираем код по косточкам**

1.  **`pinMode(BUTTON_PIN, INPUT_PULLUP);`**
    *   Всё волшебство скрыто здесь. `INPUT_PULLUP` говорит Arduino: "Используй этот пин как вход, и подключи внутри него резистор к +5V".
    *   Благодаря этому, когда кнопка не нажата, на пине всегда `HIGH`. При нажатии кнопка соединяет пин с `GND`, и значение меняется на `LOW`.

2.  **`int buttonState = digitalRead(BUTTON_PIN);`**
    *   **`digitalRead()`** — функция, которая **считывает** текущее цифровое значение (0/1, LOW/HIGH) с указанного пина, настроенного как вход.
    *   Возвращаемое значение сохраняем в переменную `buttonState` для дальнейшего анализа.

3.  **`if (buttonState == LOW) { ... }`**
    *   Классическая условная конструкция `if-else`. Она **позволяет программе принимать решения** на основе данных.
    *   **`==` — это оператор сравнения** (равно?). Не путай с `=` (присваивание)!
    *   Помни про обратную логику: `LOW` означает, что кнопка нажата, поэтому мы включаем свет.

4.  **Работа с Монитором порта (Serial)**
    *   `Serial.begin(9600);` — инициализация последовательного порта для обмена данными с компьютером на скорости 9600 бит/с.
    *   `Serial.print()` и `Serial.println()` — вывод текста и значений в Монитор порта. Очень мощный инструмент для отладки и понимания, что происходит внутри программы.

---

## **🚀 Проекты для самостоятельной работы**

Выбери проект по душе. Не бойся комбинировать идеи и создавать что-то своё!

### **🔹 Проект 2.1: "Переключатель-невидимка" (Кнопка-триггер)**
Задача: сделать так, чтобы **одно краткое нажатие** кнопки **включало** светодиод, а следующее нажатие — **выключало**. Как выключатель света в комнате.

*   **Логика:** Нужно запоминать **предыдущее состояние** света и менять его на противоположное при нажатии кнопки.
*   **Подсказка:** Создай переменную `bool ledState = false;` для хранения состояния светодиода (true — включен, false — выключен). При обнаружении нажатия кнопки меняй `ledState = !ledState;` и применяй это значение к светодиоду.
*   **Сложность:** Убедись, что одно нажатие считывается как одно действие, а не несколько. Для этого понадобится отслеживать не просто состояние кнопки, а **момент нажатия** (переход от `HIGH` к `LOW`).

### **🔹 Проект 2.2: "Светофор с пешеходом"**
Задача: создать мини-светофор для машин (красный, жёлтый, зелёный светодиоды) и пешехода (красный, зелёный). Изначально горит зелёный для машин. При **нажатии на кнопку** (пешеход нажал на переходе) светофор для машин переключается на жёлтый, затем красный, а для пешехода загорается зелёный. Через 5 секунд пешеходу мигает зелёный, затем загорается красный, и машинный светофор возвращается к зелёному.

*   **Что нужно:** 5 светодиодов (2 красных, жёлтый, 2 зелёных) и резисторы к ним. Кнопка.
*   **Подсказка:** Используй `delay()` для создания интервалов. Весь цикл работы светофора запускай только при нажатии на кнопку. В остальное время машинам должен гореть зелёный.
*   **Усложнение:** Добавь зуммер для звукового сигнала пешехода.

### **🔹 Проект 2.3: "Система счёта нажатий"**
Задача: создать устройство, которое подсчитывает, сколько раз была нажата кнопка, и **отображает это число**.
*   **Что нужно:** Кнопка и **7-сегментный индикатор** (если его нет, можно использовать 3-4 светодиода для отображения числа в **двоичной системе** счисления, как в проекте 1.3).
*   **Логика:** При каждом нажатии кнопки увеличивай счётчик на 1 и выводи его значение на индикатор.
*   **Ключевая сложность:** Нужно обрабатывать именно **нажатие**, а не состояние "зажата". Один из способов — запоминать состояние кнопки на предыдущем цикле (`lastButtonState`) и сравнивать с текущим. Если раньше было `HIGH` (не нажата), а сейчас `LOW` (нажата) — это новое нажатие.
*   **Супер-задание:** Добавь вторую кнопку для сброса счётчика на 0.

---

## **❓ Вопросы на засыпку**

1.  Объясни своими словами, зачем нужен **подтягивающий резистор** (Pull-up) при работе с кнопкой. Что будет, если подключить кнопку без него?
2.  Почему в нашей схеме с `INPUT_PULLUP` нажатие кнопки даёт значение `LOW`, а не `HIGH`? Можно ли сделать наоборот? Как?
3.  В чём принципиальная разница между проектами "Свети, пока держу" и "Переключатель-невидимка"? Какая логика обработки кнопки в каждом случае?
4.  Представь, что тебе нужно сделать так, чтобы двойное нажатие кнопки (как двойной клик мышкой) включало светодиод, а одинарное — выключало. Опиши словами, как бы ты стал программировать такую логику.
5.  Придумай **три смешных или полезных применения** для устройства "кнопка + светодиод" в твоей комнате (кроме выключателя).

---

## **📌 Критерии успеха: как понять, что ты молодец?**

*   ✅ Схема собрана **без использования дополнительного резистора для кнопки**, только `INPUT_PULLUP`.
*   ✅ Светодиод **чётко реагирует** на нажатие и отпускание кнопки.
*   ✅ В **Мониторе порта** корректно отображается состояние кнопки (`0` при нажатии, `1` при отпускании).
*   ✅ Код **чистый**, с комментариями, и в нём правильно используется `if-else` для принятия решений.
*   ✅ Ты **самостоятельно** реализовал как минимум **один** проект и понимаешь, как в нём обрабатывается нажатие кнопки.

---

## **🆘 Скорая помощь: если что-то пошло не так...**

*   **Светодиод не реагирует на кнопку, но напрямую от пина 13 работает (мигает по старой программе):**
    *   **Проверь соединения кнопки!** Самый частый дефект. Убедись, что провод от пина 2 подключён к **диагонально противоположной** ножке от той, что подключена к `GND`.
    *   Проверь, правильно ли настроен пин в `setup()`: `pinMode(BUTTON_PIN, INPUT_PULLUP);`.
*   **Светодиод горит тускло или, наоборот, не гаснет полностью:**
    *   Проверь, не забыл ли ты **резистор 220 Ом** для светодиода.
    *   Убедись, что короткая ножка светодиода через резистор идёт именно в `GND`, а не в другой пин.
*   **В Мониторе порта постоянно `0` или постоянно `1`, независимо от кнопки:**
    *   **Постоянно `0`:** Возможно, пин кнопки закорочен на `GND`. Проверь, нет ли случайного контакта провода с макетной рельсой `GND`.
    *   **Постоянно `1`:** Возможно, пин кнопки не соединён с `GND` через кнопку. Проверь цепь: пин 2 → провод → ножка кнопки → противоположная ножка кнопки → провод → `GND`.
*   **Кнопка срабатывает несколько раз от одного нажатия (дребезг контактов):**
    *   Это нормальное явление для механических кнопок. Контакты при замыкании физически "дребезжат". Для простых проектов с `delay(100)` это незаметно. Для точного счёта нажатий нужно будет использовать **"антидребезг"** в коде (это тема отдельного урока).

**Главный совет:** Сила Arduino — во взаимодействии с миром. Если что-то не работает, ищи проблему **на стыке** программы и схемы. В 95% случаев ошибка — в проводах или неверном подключении. Будь внимателен, и всё получится
